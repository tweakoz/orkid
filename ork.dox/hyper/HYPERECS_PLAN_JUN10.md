# HYPERECS — The Ordered Plan (2026-06-10)

**The execution plan for HYPERECS** — architecture/design lives in `HYPERECS.md` (same dir); item ids below (e.g. *0.1*, *2.4*) are stable internal handles for each change.
**Purpose:** the execution ordering. Architectural deficiencies first (the hard, shape-setting stuff) so the later work falls into place; every step lands green (no regressions) and is individually revertible.

---

## 0. The three ordering principles

1. **Oracles before surgery.** "No regressions per step" is only a real property if something asserts it. The round-trip gate and the CPU-vs-GPU equivalence harness are not features — they are the ratchet that makes every later stage safe. They are cheap and purely additive (zero regression risk themselves), so they go first: not because they're easy, but because they convert the hard stages from "hope" to "asserted."

2. **Contracts before construction.** The genuinely architectural deficiencies are mostly *small code with huge consequence* — the plug typeid scheme, the GraphInst env slot, the param model, the tag contract, the clock contract. Construction (GPU ports, the C++ host, cross-family modules) built on the wrong contract all gets re-touched. Locking a contract is days; reworking what was built on the wrong one is weeks. The contract cluster (Stage B) is the only truly *serializing* stage — everything after it forks into parallel streams.

3. **Shrink the surface before the riskiest change.** The non-obvious ordering call: **Pivot-1 GPU ports come *before* P3b (non-blocking submit).** The scariest coupling is that the descriptor-set ring and the COW pool free-list are only memory-safe *because of* the blocking fence (vulkan_compute.cpp:77-83) — and every CPU-readback path in subdivide/extrude/inset also silently depends on that fence (map-after-dispatch works because the wait happened). P3b first ⇒ all those readbacks need explicit sync — throwaway work, because the ports *delete* those paths. Ports first ⇒ P3b's sync surface shrinks to compact's 1-uint + bevel's one-time 4-uint read ⇒ far fewer ways to corrupt.

**Definition of GREEN (asserted at the end of every step, from Stage A item 2 onward):**
full hypermesh validate-asset set (`_ork.hypermesh.validate.py`, trimesh oracle) · terrain self-test trio (Ops/RoundTrip/Cache) · `hypermeshFoundationSelfTest` · the hypermesh gates (test_hypermesh_reflection.py + test_hypermesh_roundtrip.py — LANDED) · the equivalence oracles (once they exist, A.4) · build RC=0.

---

## Stage A — the ratchet (all S/M, additive, zero regression risk)

> **A.1 LANDED (2026-06-10).** All hypermesh op state now reflects: Select (predicate GLSL + 6 MaskOp uints +
> domain), Transform (matrix), ExtrudeFaces (5 predicate strings + expr_params + part_masks), Bevel
> (slot + part_masks), BitOp (×5), Inset (part_masks), the 5 primitives (mask triples), SortTest (n) — plus
> the **predicate ABI version** (`kPredicateABIVersion` in hmdflow.h, reflected + asserted at instance
> creation) so a serialized GLSL predicate targeting a changed shader-shell contract fails loudly.
> GATE SEED: `ork.lev2/pyext/tests/llgfx/test_hypermesh_reflection.py` asserts (1) the JSON contains every
> authored value, (2) field-level equality after deserialize (no DSL re-run), (3) byte-identical re-serialize.
> **A.2 LANDED (2026-06-10).** `Hypermesh.save(path)` + `load_graphdata(path)` + `materialize_graph()`
> (the DSL's first serialize surface) and the BAKE-compare gate
> `ork.lev2/pyext/tests/llgfx/test_hypermesh_roundtrip.py`: author→bake→OBJ, save, load with ZERO DSL
> re-run, bake the deserialized graph → **byte-identical OBJ** (raw compare per decision #1). Proves
> fields, connections, predicate GLSL, expr params, and the multi-segment extrude all reproduce the
> authored compute from JSON alone. Both gates are now part of GREEN.
> **A.3 LANDED (2026-06-10).** (i) **Plug-layout skew gate**: plug `name` now serializes, and its
> deserialize SETTER asserts-not-overwrites — a reshapeIOs reorder/rename since save aborts loudly at the
> exact plug instead of silently scrambling values (appending plugs stays compatible: no serialized entry
> -> reshapeIOs default; full name-keyed REMATCHING deferred until a real layout change demands it — B.4
> appends, so it's safe). (ii) **Fail-loud connection deserialize**: unresolvable module/plug names now
> print the edge + abort (was a silent drop -> half-wired graph); dead `_deser_connections` two-phase path
> + `DeserConnection` struct removed. (iii) **MeshEdges 64-bit key**: the honest finding — true widening
> needs a TWO-PASS stable sort + key regather (MeshSort permutes only one payload), an M not the review's
> S; landed the fail-loud 65,536-vert gate in MeshEdges::build now, widening filed with C.2's sort work.
> Negative-test gate: `ork.lev2/pyext/tests/llgfx/test_hypermesh_skewgates.py` (child-process corrupt-load:
> both protections fire; pristine control loads).
> **A.4 LANDED (2026-06-10) — STAGE A COMPLETE.** The Pivot-0 oracles:
> `ork.lev2/pyext/tests/llgfx/test_hypermesh_oracles.py`. (1) DETERMINISM: a multi-op asset baked 3 ways
> (same graphdata twice + a fresh re-trace) is BIT-IDENTICAL — decision #1's premise, now asserted (it
> underwrites every raw-compare gate). (2) The EQUIVALENCE HARNESS `compare_meshes()` (tiers:
> BYTE-IDENTICAL / EPSILON same-topology position-tolerance / FAIL-with-first-divergence), proven on the
> one op that already has both impls — inset cpu=True (CPU reference) vs cpu=False (cs_inner GPU):
> result BYTE-IDENTICAL. The Stage-C ports are accepted by exactly this harness: flip the impl switch,
> bake both, compare_meshes(). POLICY (ratified): the CPU paths of extrude/inset/subdivide are RETAINED
> as test-only reference impls when their GPU ports land.
> Gotcha for test authors: serialization walks reflected class descriptions — bare
> orkengine imports serialize EMPTY (122 bytes); use the terrain-precedent `ecs.headless_appinit` recipe,
> and wrap the body in try/finally(coreappexit) or a failed assert masquerades as a teardown hang.

*Everything here is new test infrastructure or write-only reflection — nothing existing reads it yet, so nothing can regress. Do in this order:*

| # | Item | id | Size | Why this position |
|---|---|---|---|---|
| A.1 | **Reflect ALL hypermesh op state**: Select (predicate GLSL + 6 mask uints + domain — reflects NOTHING today), Transform `_matrix` (fmtx4), Extrude 5 predicate strings + `_part_masks` + `_expr_params`, Bevel slot+masks, BitOp ×5, Inset masks, primitive `_mask` triples, SortTest `_n`. Every reflection method has an in-repo precedent (terrain `_shadertext`, ITyped<fmtx4>, directVectorProperty). | 0.1 | M | Literally first — the hole **grew this week** (the multi-segment extrude's twist/scale predicates went in pyext-only). Every day of delay adds debt. The pyext binding file is the authoritative inventory of what to reflect. |
| A.2 | **`Hypermesh.save(path)`/load** (the DSL has no serialize path at all) → then the **headless round-trip GATE**: build→bake→readback; serialize→deserialize→bake→compare stored buffers RAW (ratified decision #1 makes raw-compare valid); per-op assertions on every authored-field class. Mirror the terrain self-test trio. | 0.3 → 0.2 | S+M | The gate needs A.1 + save. From here on, GREEN includes the gate. |
| A.3 | **Pulled forward from Phase 2** (contract/corruption items mislabeled as later work): **name-keyed plug-value deserialize** (2.18 — the positional matching silently shifts authored values across engine versions; the asset library starts existing at A.2, so this must precede it accumulating) · **fail-loud connection deserialize** (0.8 — silent edge drops) · **64-bit MeshEdges key** (2.17 — silent corruption ≥65,536 verts; fix before anything depends on edge ops at scale). | 2.18, 0.8, 2.17 | M+S+S | "Before a library accumulates" means NOW — the library starts at A.2. |
| A.4 | **The Pivot-0 oracles**: retain the CPU paths of extrude/inset/subdivide as reference impls (decision: keep, test-only); CPU-vs-GPU equivalence harness + run-twice determinism gate. | 0.4 | M | The acceptance gate Stage C's ports cannot start without. |

---

## Stage B — lock the contracts (the real architecture stage; small code, all consequence; the only serializing stage)

> **B.1 LANDED (2026-06-10).** (i) The typeid asymmetry is UNIFIED: both plug sides now record
> `typeid(traits::elemental_data_type)` — the DATA that flows (plug_data.inl; was in=typeid(traits) vs
> out=typeid(data), which made any typed check reject everything). Comparing data types also correctly
> admits float-out → floatxf-in (same payload; the transformer is the in-plug's business). (ii)
> `canConnect` is REAL: type_info equality with a name-string fallback (dylib-safe) + the maxFanOut
> contract (0 = unbounded). (iii) Two-phase landing as planned: runtime default = WARN (mismatches log
> with module.plug [type] → module.plug [type] and are allowed); `ORK_DFLOW_ENFORCE_TYPED_CONNECT=1`
> rejects — and all four hypermesh gates run ENFORCED in CI while interactive apps soak on WARN.
> SOAK CENSUS: zero typed-connect warnings across all 4 hypermesh gates + terrain trio + validate runs.
> Remaining polish (with enforce-by-default): surface rejection as a Python exception through pyext
> connect instead of the OrkAssert abort.

> **B.2 LANDED (2026-06-10).** `GraphInst::_impl` is no longer a single svar slot — it is `TypeKeyedVars`
> (typeid-keyed shared_ptr map) with the SAME getShared/setShared/makeShared call surface, so every
> existing site (hypermesh MeshEnv, terrain BakeEnv, particle::Context, the pyext py::object impl via
> by-value tryAs/set shims) is unchanged — and the three family envs now COEXIST on one GraphInst.
> Mixed-family graphs are mechanically possible.
>
> **B.3 LANDED (2026-06-10).** `ork/lev2/gfx/dflow/interchange.h` — the family-neutral GPU-resource-handle
> plug home (namespace `ork::lev2::dflowgfx`). First entry: `GpuComputeImage2DData/Inst` +
> `HfImagePlugTraits` promoted OUT of terrain (which re-exports via aliases — zero churn in its ~20 TUs;
> the serialized reflection names "terrain::hfimg{out,inp}plug" are PRESERVED, terrain artifacts load
> unchanged). Hypermesh now includes it and its drivers create `hm_hfimage` registers, so a heightfield
> value can FLOW through a hypermesh graph — the substrate E.1's DisplaceByField lands on. Mesh /
> InstanceSet / SDF handles slot in here as their edges land.
>
> **B.4 LANDED (2026-06-10) — the param/animation/clock contract.** (i) **EXPRP -> real plugs**: expression
> params are a FIXED set of `kMaxExprParams`(=8) vec4 input plugs `exprp0..7` on ExtrudeFaces (the terrain
> kMaxExprInputs pattern — static plug layout, no deserialize-ordering trap; unused idle at zero). They
> serialize as plug values; the pyext `set_expr_params/set_expr_param4` setters now write the PLUGS (same
> call surface — zero DSL churn); writeParams snapshots them into the EXPRP SSBO (moved OUT of compute(),
> where the host map sat mid-dispatch-phase). The `_expr_params` vector member is GONE. (ii) **The clock
> feed**: MeshEnv gained `_abstime/_dt`; the LIVE render hook advances them (steady clock anchored at first
> frame) and fills `live->_updata` (ECS-contract parity) — the frozen-at-0.0 defect is dead; BAKE never runs
> the hook -> stays the deterministic t=0 snapshot. (iii) **The S.time bridge**: a reflected `time_slot` on
> ExtrudeFaces (-1=none; the DSL sets it when `S.time` is used) — writeParams feeds that slot's .x/.y from
> the env clock. The per-frame Python `_install_time_driver` is DELETED; S.time is now declarative
> (serializes, zero per-frame Python). Viewers treat `_time_param`-bearing assets as animated
> (`Hypermesh.is_animated`). (iv) **Cross-thread contract** (2.15): the defined channel = plug values written
> via setters on the update thread BETWEEN frames; the render thread's writeParams pre-phase host-write is
> the snapshot boundary (documented at kMaxExprParams). (v) **Time/Parameters module home — DECIDED**:
> core `ork::dataflow`, promoted from particles' Globals/ParametersModule/floatxf tier (implementation =
> plan 1.2c, scheduled with the Stage-D host work; the time_slot bridge is the interim tier-b for
> hypermesh). NEW ORACLE: `oracle[param-drive]` asserts a param value actually REACHES the shader
> (two bakes differing only in a default must differ) — the round-trip gate alone cannot see this.
> LIVE-VIEWER CHECK STILL OWED: S.time animation under the C++ clock (owner's viewer run).
>
> **B.4 addendum — CLOCK PAUSE (owner requirement: pausing dflow graphs is critical).** Pause is a HOST
> concern (the host stops advancing the clock it feeds). Implemented per-graph (`LiveHypermesh._paused`,
> pyext `live.paused`) + GLOBAL (`hypermesh::setClockPaused` / `lev2.hypermesh.set_clock_paused(bool)`).
> The clock is now INCREMENTAL (per-live accumulator): while (global || per-graph) paused, dt=0 and
> abstime HOLDS — resume continues seamlessly, no time jump. The viewer's [SPACE] now freezes BOTH the
> asset's onUpdate (plug pokes) and the global clock. The same rule carries to the ECS host (1.2c):
> pausing = not advancing the UpdateData it feeds (Simulation-level pause).
>
> **B.5 DECISIONS (owner, 2026-06-10) — three of five resolved:**
> - **(c) fxv2 artifact policy = ALIAS + per-asset-subfolder `.shaders/` dirs — IMPLEMENTED.** Generated
>   material shaders persist next to the defining material .py (e.g.
>   `hypergraph/assets/materials/terrain/.shaders/solid_<hash>.fxv2`) and serialize as the portable
>   `<hyperassets>/...` alias (expander registered in CORE app init — the future C++ host resolves it for
>   free). The staging dslshadercache remains the fallback for non-asset contexts (inline/test classes).
>   Artifacts now live + version WITH the assets instead of the wipeable machine-local cache.
> - **(d) dirty machinery = LIFT the version-counter pattern into dflow core** (per-plug monotonic
>   versions; the hypermesh `_version/_topoVersion` pattern generalized). Direction locked NOW so C.1's
>   dirty-gating is written against it; the core implementation stays plan 3.6 (with the second family
>   that needs it). The stubbed bool isDirty API gets deleted as part of that lift, not before.
> - **(e) plug-transformer semantics = ALWAYS-APPLY** (the transformer shapes the value whether the plug
>   is authored or connected). NOT yet implemented — it can change existing particle graphs that relied
>   on the accidental connected-only behavior, so it lands as its own change with a suite soak + an
>   owner visual pass over the particle demos, and ONLY THEN do transformers get reflected (A.1-style)
>   per the serialization principle. Until then transformers stay unreflected (status quo).
> - **(a) gid write-mask — LOCKED + IMPLEMENTED (2026-06-10).** Every face-tag writer is now
>   hard-masked to the free region `0x000FFFFF` in the SHADER (no Python-supplied triple can touch
>   the gid bits by construction): FaceTagger `cs_retag` inherits gid from the parent verbatim,
>   Select's face-domain RMW preserves `_tags & 0xFFF00000`, BitOp clamps its destination band to
>   the free region AND fail-louds in C++ when `dst+width > 20` (ops self-defend — never silently
>   truncate). Edge/vertex tag domains stay fully free (the gid contract is face-only). Reserved
>   verbs: `assign_gid(src, gid, slot)` exists as a loud NotImplementedError stub on the asset base
>   (the setter ships WITH E.3 render bucketing — sole gid write verb); `S.gid` read atom is LIVE
>   (`(tags>>20)&0xFFF`, valid in every predicate scope since `_tags` is a local in all of them).
> - **(b) channel-table convention — RECORDED (2026-06-10)** as a normative comment block beside
>   `MeshChannel` in `hmdflow.h`: ops iterate per-op `kCh[]` channel tables (extrude/bevel/mirror/
>   compact precedent), never name channels in op logic/shader text; channel-specific math hangs
>   off table-entry mode flags. C.4 ports MUST be written channel-generic from day one.
>
> **ALL FIVE B.5 DECISIONS RESOLVED → STAGE B COMPLETE (2026-06-10).** Open follow-ons carried
> forward: transformer always-apply implementation (own change + soak, then reflect), dirty-core
> lift (3.6), gid setter + bucketing (E.3). Next fork: **C.1** (hook move + dirty-gating — perf/racer
> story) ∥ **D.1** (C++ AssetSystem materializer — Goal-C story).

| # | Item | id | Size | Why architectural |
|---|---|---|---|---|
| B.1 | **Typeid unification → typed `canConnect`.** Unify the in/out plug typeid asymmetry FIRST (out records `typeid(data)`, in records `typeid(traits)`, plug_data.inl:25/:47 — the naive fix rejects ALL connections), then `type_index` compare + maxFanOut + a real error surfaced through pyext connect. **Two-phase landing: WARN mode first** (log would-be rejections across all family suites for a full cycle), then flip to enforce. | 0.5 | M | The most rework-prone item in the entire plan if deferred — every plug type and every cross-family edge ever made builds on connection typing. Do it while the graph population is small. |
| B.2 | **GraphInst env unification** — move MeshEnv/BakeEnv off the single `_impl` svar slot onto keyed `_vars`. | 0.6 | S | Mixed-family graphs are *impossible* until this lands; any cross-family module written before it gets reworked. |
| B.3 | **Family-neutral interchange header** — promote the heightfield plug (`GpuComputeImage2D*` + traits) as the first entry of the GPU-resource-handle set; register in hypermesh drivers. Mesh / InstanceSet / SDF handles slot in here later. | 0.7 | S | The typed-link substrate the cross-family slices (E.1/E.2) land on. |
| B.4 | **The param + animation contract** (one cluster — these define how every future asset is authored): **EXPRP→real input plugs** (1.3: vec4 plugs created in reshapeIOs from a reflected slot count — serializes as plug values, connectable by oscillators, one defined cross-thread channel; do *now* so A.1's reflected-vector form doesn't fossilize into a schema migration) · **the cross-thread poke contract** (2.15: Python writes only between frames via defined setters; snapshot at writeParams boundaries — define before no-GIL and before oscillators multiply the writers) · **the clock feed** (1.2a: the one-line C++ fill of `live->_updata->_abstime/_dt` — frozen at 0.0 today, hmdflow.cpp:195-197 — plus the writeParams bridge that deletes the Python `S.time` driver) · **decide the Time/Parameters module home** (promote particles' Globals/ParametersModule/floatxf tier to core — the tier-b declarative-animation contract). | 1.3 + 2.15 + 1.2 | M+S+S+M | Every Python-`onUpdate` asset written before this contract exists is migration debt. The particles precedent means this is promotion, not invention. |
| B.5 | **Contract-only decisions — lock now, implement later (each ~S or zero code):** (a) **the gid tag mask**: `FaceTagger`/`MaskOp` writes masked to `0x000FFFFF` + `assign_gid`/`S.gid` reserved — so nothing written in the meantime clobbers the gid bits; the *render bucketing feature* stays in Stage E. (b) **channel-table-driven convention** for the Pivot-1 ports (channel-generic from day one — the ports never get re-touched for named channels later). (c) **fxv2 embed-vs-alias** (0.9 — portable shaderpath; decide, then the S-sized fix). (d) **dirty-machinery direction** (lift the hypermesh version-counter pattern into core vs delete the stubbed bool API — decision only; implementation is Phase-3). (e) **plug-transformer semantics** (always-apply vs connected-only — decide BEFORE reflecting them). | 2.5-contract, 0.9, 3.6-dir, transformer | S each | Splitting CONTRACT from IMPLEMENTATION is what lets the implementations fall into place later with zero rework. |

---

## Stages C ∥ D — two parallel streams (B unblocks both; neither blocks the other)

*The host (D) consumes the same APIs C rewires internally; C's internals don't change D's contracts. Interleave freely.*

### Stage C — the GPU-native engine core (internal order is strict)

> **C.1 LANDED (2026-06-10), all three parts:**
> - **(a) hook relocation**: `_liveRecompute` moved to `ComputeDrawable::onGpuUpdate` (per-FRAME,
>   view-independent); `onPreRender` keeps only the camera block + view-dependent passes (cull).
>   `Scene::gpuUpdate` is now idempotent per render frame (`ctx->GetTargetFrame()` stamp) and the
>   direct `_renderIMPL` paths call it defensively — first host wins (SGVP gpuUpdateAll / ECS
>   `_onGpuUpdate` / direct renderOnContext), two viewports sharing a scene no longer double-eval.
>   NEW GATE `test_hypermesh_livepath.py`: headless renderOnContext frames assert the hook FIRED
>   (live clock advanced — no other gate exercised the live render path), pause holds, resume works.
> - **(b) dirty gate**: graph eval runs only when animated AND unpaused (pause = the graph STOPS;
>   plug edits while paused apply on unpause). Triangulation re-runs only when a composite TOPOLOGY
>   KEY moves (`_topoVersion` + counts + vidx/face_offsets COW handles — counts/handles observed
>   directly, so count-set-within-capacity paths are caught without auditing every module); the key
>   is sampled post-writeParams so the triangulate still rides the SAME dispatch phase as the graph
>   compute (one submit), with a rare corrective phase for compute-time topology rebuilds.
>   `allocMesh` now auto-bumps `markTopoChanged()` (re-emission IS the documented dirty signal; ops
>   self-defend). Net: static meshes = ZERO per-frame GPU work after frame 1; positions-only
>   animation (ripple) never re-triangulates; paused graphs stop burning GPU entirely. The livepath
>   gate asserts geometry MOVES unpaused and FREEZES paused (over- and under-skip both caught).
> - **(c) MeshEdges topology-keyed cache**: `build()` early-outs on the same composite key (the
>   EDGE table is connectivity-pure — midpoint/length/dihedral derive at use time from P/N), killing
>   the per-frame 32-pass serial sort for every edge consumer (select LINE, bevel) at once. Found
>   + fixed en route: `ensure()` zeroed the GPU-published `e_ne` count every call (masked before
>   only because build re-ran unconditionally) — it now rewrites the control block only when counts
>   move. NEW GATE `test_hypermesh_edgecache.py` (cold-vs-hit identical, 12 cube edges selected).
> - LiveHypermesh gained read-only `clock_abstime` (observability for the gates). Full sweep green:
>   6 gates + 8 demos + terrain_ops. `edge_select_test` reports BLANK by design (verification
>   points, not a renderable model) — its DATA is asserted correct by the edgecache gate.

> **C.2 LANDED (2026-06-10): parallel MeshScan, 97× sort speedup.** 3-stage block scan (per-block-thread
> partial sums → single-thread block-sum scan → per-block apply), 256 elements/block, GLOBAL memory only —
> no shared mem / no in-shader barriers (shadlang's SPIR-V path has neither), so determinism is structural
> (fixed iteration order, zero atomics). API: `scan()` gained an OPTIONAL `n` hint (host-side dispatch
> sizing needs it; default = serial fallback, all callers compile unchanged); the two hot internal callers
> pass it (MeshSort per-pass, MeshEdges) — bevel/compact/delete/mirror stay serial (small n, rebuild-only).
> Scratch is FIXED at init (16384 blocks = 4.19M element cap; over-cap → serial, correct just slow) since
> scan() runs inside a dispatch phase and must never allocate. MEASURED: 32-pass sort @ 262144 elements
> 296.8ms → 3.07ms. NEW GATE `test_hypermesh_scan_oracle.py`: SortTest at 10 sizes straddling every block/
> dispatch boundary (1…262144), asserted against numpy's stable argsort of the identical hash — exact
> end-to-end oracle. Debug knobs: `ORK_HM_SCAN_SERIAL=1` (serial scan), `ORK_HM_FORCE_DIRTY=1` (C.1b gate
> open). NOTE (pre-existing, found while sweeping): `delete_demo`/`compact_demo` validate BLANK on the
> UNCHANGED pre-C.1 build too — bake/dump data is CORRECT (642v/562f bowl); suspicion: the render path
> sizes cs_tri off HOST-side `_num_faces` while delete/compact counts settle GPU-side. File with C.3
> (indirect dispatch is the structural fix: dispatch off GPU counts).

> **C.3 LANDED (2026-06-10): `dispatchComputeIndirect` + the delete/compact "BLANK" closed.**
> - **(a) indirect dispatch**: `VkComputeInterface::dispatchComputeIndirect(shader, args_ssbo, offset)`
>   — vkCmdDispatchIndirect off a GPU-written VkDispatchIndirectCommand; signature CHANGED from the
>   dead `int32_t*` stub (zero callers) to the SSBO form mirroring DrawIndexedIndirectEML; DEFAULT
>   SSBOs already carry INDIRECT usage. Same fresh-descriptor-set-per-dispatch handling as
>   dispatchCompute. pyext: `ci.dispatchIndirect(shader, args, offset=0)` + `ci.storageBarrier()`
>   (was unbound) + `mapping.writeBytes(data, offset=0)` (mappings were read-only from Python).
>   NEW GATE `test_compute_indirect_dispatch.py`: cs_seed writes COUNT=777 GPU-side → cs_writeargs
>   derives {ceil(777/64),1,1} → indirect dispatch marks exactly 777 elements; host never knows the
>   count. C.4 ports can now dispatch over GPU-side counts (scan totals, live edge counts) readback-free.
> - **(b) delete/compact BLANK root-caused — NOT a render bug.** The C.2-era hypothesis (host-count
>   sizing) was WRONG: delete keeps host nf with zero-length CSR entries; the frame capture shows the
>   bowl rendering CORRECTLY. Actual cause: the validate harness's appear-check required >=2% of
>   sampled pixels to differ from background; the open bowl reads 1.4% (dim interior, mostly hole) →
>   false-negative BLANK. Fix: NB_MIN 0.02 → 0.005 (true blank = 0%; flat fills still rejected by
>   SPREAD_MIN — no safety lost). delete_demo + compact_demo now PASS; edge_select_test stays BLANK
>   by design (integer-coordinate verification points; its data is asserted by the edgecache gate).
> - Found en route: the legacy `lev2.lev2appinit()` ad-hoc lifecycle SEGFAULTS at gfx init in the
>   current tree (test_compute_shared also crashes) — pre-existing; gates use `ecs.headless_appinit`.

> **C.4a LANDED (2026-06-10): subdivide UNIFORM GPU topology build + the 2.7 staleness fix.**
> - **GPU build** (linear uniform whole-mesh path): the per-level tables `_buildLevel` derived on the
>   CPU (after a FULL vidx/fo readback) now derive ON the GPU — `MeshEdges::buildRaw` (new raw-buffer
>   overload, no caching; the cached `build()` wraps it) gives the unique-edge table + the corner→edge
>   map (midpoint vert id = src_nv + corner_edge[c], exactly the C.4 thesis), then count→4×scan→emit
>   (the delete/compact pattern) produces the refined CSR/vidx + center tables. The ONLY readback is
>   5 TOTALS (20 bytes) per level per topology-CHANGE; steady frames stay positions-only. Interp
>   shaders for GPU-built levels take counts from a per-level ctl SSBO (slot 14) — compiled ONCE,
>   rebuilds never recompile. Smooth/CC + masked stay CPU-built (welded cage / tag propagation),
>   selected per-build; `ORK_HM_SUBD_CPU=1` forces the CPU reference.
> - **Canonical edge numbering on BOTH paths** (sorted (min,max) key — matches MeshEdges' sort order;
>   CPU `_buildLevel` switched from first-encounter order, ecL/ecR now indexed by the canonical emid):
>   this is what makes CPU- and GPU-built topology BYTE-IDENTICAL.
> - **2.7 staleness FIXED** (both paths): `writeParams` adopts the extrude defer-one-frame pattern —
>   producer re-emits topology → ack + defer → rebuild against settled buffers next frame. SubLevel
>   rebuild hygiene clears per-path state; pooled tables re-acquire only on pow2 growth (no leak).
> - NEW GATE `test_hypermesh_subdiv_oracle.py`: 8 GPU-vs-CPU byte-identical cases (pure-tri ico,
>   pure-quad box, mixed tri+ngon cone, mixed quad+tri uvsphere, levels 1+2, chained
>   extrude→subdivide) + GPU determinism + the 2.7 case (LIVE cone sides 7→11 through subdivide ==
>   a fresh bake at 11). Suite: 9 gates + 8 demos green.
> - C.4 REMAINING (strict order): extrude face-mode port → inset port (each behind this same oracle
>   pattern, individually revertible).

> **C.4b LANDED (2026-06-10): extrude FACE-mode GPU topology build.** Face mode is per-face
> independent — NO edge table, NO sort: the CPU rebuild (full vidx/fo/tags/P/N readback + emission)
> became count → 3 scans → emit with tags read ON the GPU. Three exact-fit shader modules: cs_xcnt
> (per-face dup/corner/face counts, single + multi-segment), cs_xtopo/cs_xzlift (refined CSR/vidx/
> parent/partition + the zero lift table), cs_xdups (the per-dup float tables — face normal,
> centroid insets, edge tangents — computed in-shader from POSITION with the CPU's exact
> expressions, manual x*(1/L) normalization). ONLY readback = the 3 scan totals (12 bytes) per
> topology-change rebuild. Vertex/region mode keeps the CPU build (boundary classification needs
> the edge map — later). `ORK_HM_EXTRUDE_CPU=1` = the reference. The per-frame path (cs_field/
> cs_face/cs_orig/cs_dup + EXPRP) is UNCHANGED — same tables, same kernels.
> NEW GATE `test_hypermesh_extrude_oracle.py` (6 cases: single, keep_base, mixed tri+ngon cone,
> multi-segment with twist/scale S.t expressions, expression distance, chained extrude→extrude):
> TOPOLOGY byte-exact everywhere; float tables within 1e-6 (4/6 fully byte-identical — CPU↔GPU FMA
> contraction differs by ~1 ulp; unlike subdivide, the float tables here are computed on different
> processors, so the oracle is topo-exact + float-tolerance by design). Suite: 10 gates + 10 demos
> green (droop_demo_oct = 16 chained extrudes through the GPU build; racer full stack).
> C.4 REMAINING: the INSET port (2.6: `_cpu=false` default, GPU collar, kill the per-crossing
> full-mesh postCheck readback) — the racer's worst remaining CPU path.

> **C.4c LANDED (2026-06-10): the INSET port — STAGE C.4 COMPLETE (all three GPU ports).**
> - **GPU build**: count → 6 scans → emit (3 exact-fit modules: topo/dups/blob) + **the Fuchs/Kedem
>   DP collar as a kernel** (`cs_icollar`, one thread per collar face, DP in global scratch sized by
>   its own scan) — run at rebuild (rest angles) AND per rotate change. Readbacks: 6 scan totals
>   (24B)/rebuild + a **4-byte changed-flag on rotate-change frames, replacing the per-crossing
>   FULL-MESH postCheck readback** (collar validity = the monotone-staircase proof + the vet
>   harness). `_cpu` default flipped to **false** (C++ + DSL): GPU build + cs_inner positions are
>   now the production path; `cpu=True` (or `ORK_HM_INSET_CPU=1`) = the retained CPU reference,
>   which keeps its pre/postCheck suites.
> - **Cross-processor determinism, the real C.4c lesson**: discrete choices fed by floats DIVERGE
>   across processors at ties — hit twice: (1) the nominal-source nearest-vert pick (45°-symmetric
>   ring = exact tie; fixed with a 1e-6 tie margin on BOTH paths, first-index wins), (2) the DP
>   cost comparison (m=4/N=6 near-symmetric ties; fixed by QUANTIZING the angles entering the DP
>   to 1e-5 rad on BOTH paths — identical inputs + identical IEEE op order ⇒ bit-identical DP on
>   CPU and GPU, ties included). Also: `PI2` is a shadlang-predefined constant — redefining it in
>   shader text KILLS the parser with no error message (dump + SIGTRAP); literal inlined.
> - NEW GATE `test_hypermesh_inset_oracle.py` (7 cases): plain 1:1, resampled N-gon (the DP),
>   no-fill hole (CPU reference needs postcheck=False — its edge-health contract correctly rejects
>   intentional boundary edges), rotate-at-build, ngon source, chained inset→extrude — ALL
>   topology byte-exact, floats ≤1e-6 — plus the LIVE rotate spin across sector crossings (the GPU
>   adaptive collar) == fresh bakes. Suite: 11 gates + 12 demos green (racer + inset_demo on the
>   GPU build).
> **STAGE C.4 DONE → next: C.5 (P3b non-blocking submit, flagged) or D.1 (C++ materializer).**

> **C.5 LANDED (2026-06-10): P3b non-blocking submit — FLAGGED (`ORK_HM_NB_SUBMIT=1`), blocking
> remains the soak-default.** Depth-1 overlap: `endDispatchPhase` submits on the persistent fence
> and RETURNS; the wait moves to `syncPendingDispatch()`, called at every hazard point — the next
> `beginDispatchPhase` (command-buffer reset + the descriptor-ring generation recycle + COW-pool
> reuse all become safe there) and ANY host `mapStorageBuffer` (param rewrites, readbacks: the
> funnel that makes the co-land list — fence-gated descriptor recycling, fence-gated pool reuse,
> param protection — fall out of ONE wait point instead of three mechanisms). GPU→GPU ordering vs
> the render costs nothing: same VkQueue, submission order + the end-phase barrier (which also
> gained the long-missing INDEX/INDIRECT_COMMAND_READ dst bits — DrawIndexedIndirect consumed
> compute-written args under a barrier that didn't name them). The depth-1 design deliberately
> SKIPS N-buffered params + phase coalescing (less critical once submits don't block; coalescing
> remains future work if profiling demands it). SOAK: 11 gates + 6 demos green with the flag ON;
> defaults re-verified green; perf parity on the map-chained probe (the benefit accrues to the
> render loop, where the hook's submit no longer stalls before the frame encodes — owner-owed:
> a viewer feel pass with the flag ON). Flip the default only after that soak.

> **D.1 STAGE 1 LANDED (2026-06-10): the C++ PbrMaterialGenData materializer.**
> `PbrMaterialGenData::materialize(Context*)` (asset_gen.cpp) is a 1:1 port of the Python
> build(): images from paths (procedural solid-color defaults otherwise), factors + all PBR2
> lobes mirrored, the generated-fxv2 `_shaderpath` set PRE-gpuInit (resolves through the
> <hyperassets> alias — the B.5c decision paying off exactly as designed), the bindable
> `_shader_params` pre-bound POST-gpuInit (the varmap value IS bindParam's varval_t — zero
> conversion). pyext: `gendata.materialize(ctx)`. The Python wrapper DELEGATES by default
> (`ORK_HM_PYMAT=1` = the retained Python reference, removed only at parity per the plan), so
> EVERY existing render exercises the C++ path. PROOF: static-asset renders C++-vs-Python are
> PIXEL-IDENTICAL (diff bbox None); 12 gates + 8 demos + terrain green. NEW GATE
> `test_hypermesh_matgen.py`: gendata → JSON → deserialize (no DSL) → C++ materialize → live
> material (the Goal-C shape for materials), ptex3d-shaderpath + plain-factor cases + parity.
> D.1 REMAINING: HeightFieldGenData (paths/manifest around the already-C++ bake) → the wire
> step (AssetSystem::_onStage registry; Python wire_scene_data delegates per-asset-type).

> **D.1 STAGE 2 LANDED (2026-06-10): the C++ HeightFieldGenData materializer.**
> `HeightFieldGenData::materialize(Context*, ext)` (asset_gen.cpp): derives per-channel output
> paths onto the graph's CaptureModules (single → concrete path, multi → {channel} template),
> runs the cook-cache-backed `bakeHeightfield`, writes the `.terrain.json` scale-contract manifest
> (TerrainManifest schema v1, full float precision) and returns the manifest path — the manifest
> IS the result contract. The Python wrapper delegates (rebuilds its result dict FROM the
> manifest); the AUTHORING-only scatter sinks stay Python-side by design (deserialized scenes
> reference the .ogeo artifacts). `ORK_HM_PYMAT=1` = the retained reference. PARITY: channel EXRs
> are PIXEL-identical across both paths (raw bytes differ — the EXR encoder embeds
> nondeterministic header metadata even run-to-run, newly characterized; the A.4 determinism
> oracle compares OBJ text so this was invisible); manifests value-equal. FIX en route: outdir
> must use `file::Path::expandPathString` — a bare `toAbsolute()` renders WITHOUT the leading
> slash and Python's abspath() then doubles the CWD on (the scatterdemo viewer crash).
> ~~PRE-EXISTING: test_terrain_hfbake RED~~ — CORRECTED (2026-06-10): that bisect was contaminated
> by stray files from the broken intermediate build (which also left a literal `<assetcache>/` tree
> in the repo — cleaned; writer-side defense added: the hfdflow capture flush now routes paths
> through `Path::expandPathString`, so an unexpanded capture path can never create literal dirs).
> The REAL pre-existing defect: the ORDER-FRAGILE stats zip — `bakeHeightfield` returns FieldStats
> in FLUSH (topo) order while callers enumerate captures in NAME-SORTED module order
> (`GraphData::module(i)` is an orklut). FIXED structurally: FieldStats gained `_channel` (stamped
> at both flush sites, pyext `.channel`); the C++ manifest writer AND the retained Python reference
> key stats BY NAME, never by index. hfbake GREEN on both paths; full terrain suite green.
> D.1 REMAINING: the wire step (AssetSystem::_onStage registry; wire_scene_data delegates).

> **D.1 STAGE 3 LANDED (2026-06-10) — D.1 COMPLETE.** The C++ wire step:
> `AssetSystemData::materializeAll(Context*, VarMap&)` walks `_gens` in declaration order
> (= dependency order, the contract the Python registry documents), materializing every
> C++-supported type by name — PbrMaterialGenData → live PBRMaterial, HeightFieldGenData → the
> manifest path — into a name→artifact registry; unsupported types are SKIPPED with a notice
> (the Python wire path covers them during the transition, removed per-type as their
> materializers land). The runtime `AssetSystem` runs it ONCE at `_onGpuInit` (the first
> GPU-capable lifecycle moment, mirroring where the Python wire runs) into `artifacts()` — the
> registry the D.3 HypermeshComponent / D.5 C++ host resolve their name-references from.
> pyext: `AssetSystemData()` ctor + `materializeAll(ctx)` → dict. NEW GATE
> `test_hypermesh_wirestep.py`: an AssetSystemData carrying a ptex3d material gen + a terrain
> heightfield gen → JSON → deserialize (no DSL, no wrappers) → ONE C++ call wires the whole
> list (live material with resolvable params + parsed manifest with existing channel images).
> Note: Python's `materialize_from_scenedata` needs no further delegation — stages 1–2 already
> routed its per-type wrapper builds through the C++ materializers.
> NEXT: D.5 (the pure-C++ HYPERECS host demo — the Goal-C acceptance gate).
> D.2 + D.3 + D.4 landed 2026-06-11 (see their rows below).

| # | Item | id | Size | Notes |
|---|---|---|---|---|
| C.1 | **P3a: move `_liveRecompute` to `Drawable::onGpuUpdate`** + dirty-gate on `_version/_topoVersion` (static meshes stop re-triangulating; per-viewport duplication ends; `onPreRender` keeps deliberate per-view work). Plus **MeshEdges topology-keyed caching** (1.12 — kills the new per-frame 32-pass serial-sort cost cheaply, independent of C.2). | 1.1 + 1.12 | M+S | Contained relocation; the A-stage gates catch any behavioral drift. |
| C.2 | **Pivot 2: parallelize MeshScan** (per-workgroup local scan + block sums + add-back; API held constant — zero caller changes; serial fallback for tiny n). Oracle-gated. | 1.8 | M | Now a per-frame cost via bevel/edge-select; also speeds MeshSort (1-bit radix rides the scan). |
| C.3 | **Pivot 5: `dispatchComputeIndirect`** (vkCmdDispatchIndirect off a GPU-written args buffer; stub today, vulkan_compute.cpp:542-545). | 1.9 | M | Prerequisite so the ports dispatch over GPU-side counts instead of re-introducing readbacks. |
| C.4 | **Pivot 1: the GPU ports — one op at a time, each behind the A.4 equivalence oracle, each individually revertible.** Order: **subdivide UNIFORM** (edge midpoints = MeshEdges output; also fix the never-rebuilds `_built` staleness, 2.7) → **extrude face-mode** (per-face independent, trivially parallel; includes the multi-segment branch) → **inset (+2.6: `_cpu=false` default, GPU collar, kill the per-crossing full-mesh postCheck readback)**. Channel-generic per the B.5(b) convention. Bevel is the proven 16-kernel template. | 2.1 + 2.7 + 2.6 | XL | The racer's animated inset is the single worst remaining CPU path — landing inset here is also the biggest racer-visible win. |
| C.5 | **Only now P3b: non-blocking submit + frame overlap** — timeline semaphores; **must co-land** fence-gated descriptor-ring recycling + fence-gated COW-pool free-list + N-buffered param buffers; **phase coalescing** (batch all onGpuUpdate compute into ONE submit; fold the legacy `LiveHypermesh::recompute` second entry in). **Two-phase landing: behind a feature flag with the blocking path as fallback until it soaks.** | 2.2 | L | After C.4 the readback surface is minimal — the fewest ways to corrupt. This is the riskiest single change in the plan; the flag is the no-regression mechanism. |

### Stage D — the HYPERECS host spine (mostly mechanical once A+B exist — the "falls into place" stream)

> **D.0 (LANDED 2026-06-10) — ECS scenegraph hook plumbing.** Verified: an ECS-OWNED scene fired NEITHER
> drawable hook — `Scene::gpuUpdate` was only fanned out by `SceneGraphViewport::gpuUpdateAll` (SGVP-attached
> scenes only) and `Scene::preRender` had its single call site in the SGVP widget, while the ECS renders via
> `SceneGraphSystem::_onRender → renderOnContext` (no SGVP). So hypermesh/instance-cull drawables inside an ECS
> scene would never tick. FIXED: `SceneGraphSystem::_onGpuUpdate` now calls `_scene->gpuUpdate(ctx)` (same
> compute-legal phase — Controller::gpuUpdate rides ezapp onGpuUpdate), and `Scene::_renderIMPL` fans out
> `preRender` with the compositor-resolved camera (mirrors the SGVP dance; no double-fire — the SGVP path
> bypasses `_renderIMPL`). REMAINING GAP: the `renderWithStandardCompositorFrame` ECS path (`sframe->render()`)
> still lacks the per-view fan-out — close it when D.3's HypermeshComponent picks its render route.
> **FIXED 2026-06-11 (the ren_saddle2 regression, part 2):** the `SceneGraphSystem::_onGpuUpdate` fan-out was
> NOT "compute-legal" on the WINDOWED ezapp loop — `Controller::gpuUpdate` fires from the app `_onGpuUpdate`
> callback BEFORE `SlotRepaint`, i.e. OUTSIDE beginFrame: no primary command buffer (`LightManager::gpuInit`'s
> texture-array upload null-derefs at vulkan_txi_from_array.cpp:517) and the early call also claimed the
> per-frame dedup slot, starving the correct in-frame call. Fix: the system hook fans out ONLY when
> `ctx->_currentPhase != 0` (in-frame hosts keep the early fan-out); `renderWithStandardCompositorFrame` gained
> the same defensive in-frame `gpuUpdate(context)` call `_renderIMPL` already had (first caller per frame wins)
> — which also closes the sframe-path gpuUpdate half of the gap noted above (per-VIEW preRender still open).

| # | Item | id | Size | Notes |
|---|---|---|---|---|
| D.1 | **C++ `AssetSystem::_onStage` materializer**, staged: PbrMaterialGenData first (pure port of the Python build — every callee already C++), then HeightFieldGenData (paths + manifest around the already-C++ bake), then the wire step. Python `wire_scene_data` delegates during transition (the no-regression mechanism: old path stays until parity). | 1.4 | L | The single biggest blocker between "scene JSON deserializes in C++" and "scene renders in C++". Insertion point already named in-code. |
| D.2 | **(LANDED 2026-06-11) Particles → model B**: reflect `ParticlesDrawableData::_graphdata` + `_poolSize` + Parameters `_defaults`; embed graphdata at tojson (DSL runs ONCE at authoring); dsl_file kept as provenance; GATE `test_particles_modelb.py` (embed+reflect / byte-identical graph round-trip / build with DSL resolution POISONED / drawabledata round-trip). This was the FIRST particle-graph deserialize ever — it surfaced (a) linker-stripped reflection registrations (biasxf/scalexf/modxf/fquatxf/vect4inplug/quatinplug/GradientV4/`inplugdata<quatxf>` now touched in reflection_init; an UNTOUCHED class serializes with an EMPTY class string — grep the JSON for `"class": ""` to find the next one) and (b) a structural gap for DATA-DRIVEN plugs: `reflect.no_instantiate` arrays assume the ctor pre-built every element, but Parameters builds plugs from `_defaults`. Fix: generic `reflect.no_instantiate.fallback` property annotation (ObjectProperty.h fn-ptr type; JsonDeserializer retries the slot after invoking it; ModuleData wires it to strip IArray's null-resize padding + run the class's idempotent `reshapeIOs` — derived-class properties deserialize before the base-class plug arrays, so `_defaults` is already live). Determinism: `_defaults` is now `std::map` and `addFloatParam` keeps param plugs NAME-SORTED (plug values deserialize positionally per the skew gate, so authored order must equal rebuild order). Sweep: all 22 gates + terrain suite green. **AMENDED 2026-06-11 (ren_saddle invisible particles):** cross-asset systems (`asset_kwargs`, e.g. col_vdb's `collision_sdf` — a LIVE openvdb FloatGrid injected into VdbCollider at trace time) briefly fell back to model A: the grid can't serialize, the embedded graph round-tripped GUTTED, and build() preferred it over the DSL re-run. **SUPERSEDED same day by the SDF ASSET-REFERENCE plug — cross-asset systems are now FULLY model B:** `VdbColliderModuleData._sdf_asset_name` (reflected "sdf_asset"; the live `_sdfGrid` stays runtime-only) carries the AssetSystem NAME of the SDF asset; stamped AUTOMATICALLY at trace time (the wrapper registers {live artifact → asset name} in `dflow/particles/_artifact_names` around the DSL run; the `vdb_collider` op builder consults it — scene + DSL author surfaces unchanged); re-resolved post-deserialize by `lev2.particles.resolve_sdf_assets(graphdata, name, grid)` from `build()._resolve_asset_refs` (artifacts via from_gendata; declaration order = dependency order). The D.5 C++ host resolves the same field from the AssetSystem VarMap (needs the ImplicitSdf C++ materializer). GATE `test_particles_sdfref.py` (embed+stamp / byte-identical round-trip / build-from-clone with DSL resolution POISONED + grid re-resolved). The same {register → stamp → resolve} pattern is THE template for any future non-serializable trace input. **Second live object, same day (invisible blobs at scene start):** `VdbLevelSetRendererData._material` is a LIVE lev2 material (built imperatively in col_vdb via createPbrMaterialWithColor + lobe setters) — runtime-only, silently dropped on round-trip → `_render`'s null-material early-out = invisible particles. Fix: the renderer carries a reflected **`_material_gen`** (PbrMaterialGenData — the D.1 recipe, "material_gen"), lazily materialized at first render onto a per-INST cache when the live material is absent; `voxel_size` reflected too (kernel enum deferred — WYVILL default matches). col_vdb authors BOTH (live for in-process parity incl. its rasterstate poke, gen for the round-trip; the gen's kwargs ctor applies the same implicit has_<lobe> enables). LESSON: model B exposes EVERY live object a DSL hands a module — audit a system's modules for unreflected runtime state the first time it round-trips; the silent failure mode is always "renders nothing". | 1.5 | M | Kills the named anti-pattern (recipe → re-run Python at load). |
| D.3 | **(LANDED 2026-06-11) HypermeshGenData + HypermeshDrawableData + HypermeshComponent.** `HypermeshGenData` (asset_gen.h): embedded graph + vtx_budget, `materialize(ctx)` → LiveHypermesh; AssetSystemData::materializeAll grew the case (artifact = the SHARED live mesh). `HypermeshDrawableData` (hypermesh/hm_drawable.h/.cpp): reflected {graph, material_asset (BY NAME), animated/face_viz/tag_viz/wireframe, vtx_budget, instance_matrices, instance_source (E.2 slot)}; `createDrawable()` = the C++ make_drawable port with a LAZY bootstrap — the GPU side materializes on the FIRST onGpuUpdate (outer `_liveRecompute` closure stays installed forever and forwards to the setupMeshRender-built inner hook; reassigning the std::function you're executing is UB). Material resolves via `_resolved_material` or a polled `_material_resolver` (the AssetSystem registry may populate a frame after stage time). ENABLER: `materializeLive` now wraps its one-time setup frame ONLY when not already in-frame (`ctx->_currentPhase == 0`) — the bootstrap lands mid-frame where dispatch phases are already legal but nested beginFrame asserts. ECS: HypermeshComponent/HypermeshSystem clone the Particles stage contract (stage = createDrawable + resolver wire + sgnode attach via renderops; PAUSE/RESUME → live->_paused per B.4); NO update-thread compute (hypermesh computes in-frame on the render thread) — the full ECS-fed UpdateData clock arrives with the 1.2c Time modules. Python: `assets.Hypermesh` wrapper (embed at authoring) + `drawable_data(material=...)`. GATE `test_hypermesh_hostdata.py` (gen round-trip + materialize-from-clone / drawabledata round-trip / one-call wirestep over material+mesh / render-from-DESERIALIZED-data with lazy bootstrap + anim + per-graph pause) + ECS scene-roundtrip smoke. Sweep: all 24 gates green. NOT yet exercised: a full ECS Simulation run hosting the component (that is D.5's acceptance gate). | 1.6 | L | The HYPERECS core artifact. |
| D.4 | **(LANDED 2026-06-11) Serialize the remaining bindings.** (1.10) `ScatterSinkData` (asset_gen.h, Object-derived, touched in lev2_init): density/count (0 = the unset sentinel of the XOR pair), seed/align/yaw/scale/cutoff/jitter/max_points/lift + ORDERED `type_names`/`type_channels` (type_id = index) + the NEW reflected `type_assets`/`type_materials` string maps (asset names into the AssetSystem registry); `HeightFieldGenData._scatters` is a directObjectVectorProperty of them. Authoring: `scatter()` gained optional `assets=`/`materials=` kwargs (wrapper-or-name; validated against declared types; ScatterSpec extended with defaulted fields); the HeightField wrapper reflects `inst._scatters` → `gendata.scatters` at embed time; `_run_scatters` now reads the REFLECTED sinks first (live-DSL fallback retained) — so a build from a deserialized gendata RE-PLACES the ScatterSet (.ogeo) with zero Python DSL state (before D.4 a round-trip lost ALL scatter). The weight fields were never the problem — they ride the embedded graph as captured channels. Placement (scatter.place) stays Python until the E.2 C++ placer. (1.11) `PbrMaterialGenData._sampler_textures` (directMapProperty, sampler uniform → image path; alias-form OK — terrain-channel paths under `<assetcache>` are deterministic hence machine-independent): the C++ materializer expandPathString → Image::createFromFile → Texture + TXI initTextureFromImage → bindParam (the bound varval keeps the texture alive); missing file/param skip LOUDLY (AssetSystem declaration order = dependency order). Ptex3d wrapper gained `sampler_textures=`; the ORK_HM_PYMAT Python reference binds in parity. GATE `test_hyperecs_d4_bindings.py` (scatter reflect / roundtrip / placement-from-clone with bindings, sampler roundtrip / C++ materialize+bind). Sweep: all 25 gates green. | 1.10 + 1.11 | M+M | Author surface unchanged; pure reflection. |
| D.5 | **(HOST DEMO LANDED 2026-06-11 — Goal C runs.)** `ork.ecs.player.exe` (ork.ecs/examples/c++/player, the C++ lowering of ork.ecsplay.py): deserializeJson(.ecs) → `ecs::materializeAndWireScene` → Controller bind/create/start → per-frame update + gpuUpdate + render. **Owner-verified on ren_saddle: window renders, particles fire, 111 FPS, zero crashes, ZERO Python in the process.** What landed with it: (a) the remaining C++ materializers — `materializeImplicitSdf` (AX voxelize → FloatGrid), `materializeVdbGridToDrawable`, `materializeParticleSystem` (embedded graph + sdf_asset resolution from the registry + probe stamp) in new TU `lev2/gfx/asset_gen_vdb.{h,cpp}`; HdriToXir resolves its deterministic path (no re-bake); `AssetSystemData::materializeAll` covers all of them; (b) `ecs::materializeAndWireScene` (AssetSystem.cpp) = the C++ wire_scene_data: by-name patching of SG node items (drawable + asset:// envmap), ParticlesComponentData, and the SceneGraphSystemData skybox → _userParams; (c) **first C++ subsystem-based (HFSM) startup** — `init_data->_use_subsystems + _enabled_subsystems={opq,core,gpu,lev2}` works windowed from C++. HOST-AUTHOR CONTRACT discovered en route (trace-ecs's main_writer carries all three latent bugs): (1) ecs::initModule BEFORE OrkEzApp::create (InitializeClasses fires in create's finalize; push your own StringPoolContext first — pure-C++ hosts don't get the pyext one), (2) `findSystem<T>` only AFTER startSimulation (the deferred ref registers at composition), (3) systemNotify payloads are `shared_ptr<DataTable>` (handlers getShared; by-value reinterprets as a pointer and crashes) with EXACT-typed svar values (`45.0f * DTOR` is double; get<float> asserts). ALSO FIXED: `ork_ecs_target_opts_linker` INSTALL_RPATH was torch-only, clobbering the staging-lib rpath — EVERY ecs exe was dyld-broken (incl. trace-ecs). SAME-DAY ADDITIONS: (d) the player grew the ecsplay input surface — EzUiCam mouse/trackpad orbit (events via ezapp onUiEvent; camera fed to UpdateCamera from `uicam->_camcamdata`), Cmd+Right = RESTART (fresh Controller on the same wired scenedata — controllers are NOT reusable, the old one parks in a dead list, Python parity; gpu init rides the per-frame gpuUpdate FSM, no explicit gpuInit), Cmd+Down = stop, Space = host-side pause (the update tick holds; camera notify holds too or events queue unserviced); controller swaps guarded by one mutex across update/gpuUpdate/draw. (e) `materializeAndWireScene` now supplies RENDER DEFAULTS (the Python ensure_scenegraph_system analogue, author-wins): editor-authored scenes (ecsscn3) carry EMPTY userparams and threw "unknown compositor preset type" → abort; absent keys get preset=ForwardPBR + intensity/ambient defaults (`SceneGraphSystemData::hasUserSceneParam` added) — AND the standard LAYER set (std_forward/std_transparent/hud_overlay) when `declaredLayers()` is empty: **a ForwardPBR scene REQUIRES std_forward** (Scene::findLayer hard-asserts on a miss at component stage). (f) **HOST-AUTHOR CONTRACT #4 (the ecsscn3 grey-screen deadlock):** `controller->update()` can synchronously WAIT ON THE RENDER THREAD (`Simulation::_runGpuPhaseOnRenderThread` during composition/model load) — NEVER hold a lock across it that the render-thread hooks (gpuUpdate/draw) also take. The player uses copy-the-shared_ptr-under-lock; the mutex only guards the controller swap. VERIFICATION LESSON: "process running + zero crashes" is NOT "rendering" — assert FRAME EVIDENCE (the FPS print) in headless checks; the hang shipped past a 30s check that lacked it. ecsscn3 (classic models/lights/bullet/boids/sound scene, zero assets) now renders ~59 FPS in the C++ host. (g) **ork::debugserver** (ork.core/util, zmq REP at ipc:///tmp/ork.debugserve.<sid>.ipc; env opt-in ORKID_DEBUG_SERVER=<sid>, auto-started by EzAppContext): in-process probes queried via `ork.debug.lldbclient.py --debugserver --sessionid <sid> <probe>`; built-ins help/pid/uptime/threads (per-thread name/state/cpu via mach); apps add probes via `debugserver::registerHandler`. (h) **restart viewport fix**: a restarted controller's compositor inits tiny and no resize event fires mid-run — the player samples mainSurface dims per-frame and re-sends UpdateFramebufferSize after restart. (i) **ENGINE PAUSE finished** (owner-verified): pause = "still renders LIVE, nothing moves but the ui camera". The half-built FSM PAUSE state (blanked DB, asserting resume) became: PAUSE→ACTIVE legal; ACTIVE onEnter SKIPS re-activation when resuming (pause is a CLOCK op — re-running _activate would re-fire component onActivate); paused._onupdate = _serviceEventQueues + _update_SIMSTATE, whose PAUSE branch zeroes dt (the documented clock contract) and updates ONLY systems with the new **`System::updatesWhilePaused()` trait** (SceneGraphSystem → true). KEY INSIGHT: the renderer reads the CAMERA from the PUBLISHED draw buffer — keeping the frame "live" while paused REQUIRES the scenegraph system to keep enqueueing (camera-only freshness); merely servicing UpdateCamera events updates scene state the renderer never sees. New `Controller::pauseSimulation()/resumeSimulation()`; the player's Space uses them and keeps ticking controller->update() throughout. **+HYPERMESH HOST STAGE LANDED (2026-06-11, owner-verified):** `ren_hypermesh.py` (ork.data/scenes) — an S.time-animated extrude hypermesh + ptex3d material as scene assets, hosted by HypermeshComponent, playing VISIBLY in ork.ecs.player.exe (all GPU mesh kernels compile + dispatch in the pure-C++ host). Landed with it: (1) scenes MUST declare the hosting system (`self.system_data("HypermeshSystem")`) — components only link to declared systems; the component now fails LOUDLY instead of crashing on a missing one; (2) authoring is LAZY — `assets.Hypermesh.build()` returns None without an explicit ctx (an eager materializeLive on the loading context would race the loader thread; the host materializes at load); `_GENDATA_TO_WRAPPER` gained HypermeshGenData for the Python wire; (3) **THE LAYER TRAP**: the forward compositor renders a FIXED role set (`depth_prepass, std_forward, std_transparent, std_editor, probe, depth_probe, hud_overlay` — fwdnode_impl_top k_roles); the SG system's `_default_layer` ("sg_default") is NOT in it — a drawable attached there exists, computes, and silently never draws. HypermeshComponent now defaults to "std_forward" (idempotent createLayer find-or-create). AUDIT NOTE: anything else attaching to `_default_layer` (particles do!) deserves the same scrutiny — sort out WHY particle drawables on sg_default are visible (different draw path?) or migrate them. **GATE 1.7 LANDED (2026-06-11) — the LIVE round-trip keystroke:** `Cmd+R` in ork.ecs.player.exe (or `--roundtrip <secs>` scripted) serializes the RUNNING scenedata on the render thread, deserializes a clone, **BYTE-COMPARES the clone's re-serialization** (the standing serdes auditor — any unreflected/unstable live state shows as a one-keypress diff), re-materializes+wires the clone, and the update thread's restart adopts it — fresh simulation from `deserialize(serialize(live_scene))`, still animating. VERIFIED: saddle2 (113,299 bytes byte-identical) and ren_hypermesh (18,003 bytes byte-identical, animated graph survives) — this is the exact save/reload mechanism ecsedit can now trust. **+TERRAIN HOST STAGE LANDED (2026-06-11; E.6's chunk drawable PULLED INTO D.5 per owner call) — D.5 IS CLOSED.** `TerrainChunkDrawableData` (lev2/gfx/terrain/terrain_chunk_drawable.{h,cpp}): the C++ buffer-side port of terrain/gpu_chunk.py's ComputeDrawable consumer — reflected {hf_asset, material_asset, chunk}; D.3-pattern lazy bootstrap parses the manifest (rapidjson), OIIO-reads the height EXR, builds the [CamBlk|args|vis-list|heights] SSBO (layout arithmetic in LOCKSTEP with gpu_chunk.py — the GLSL side's single source of truth), and assembles the 3-pass reset/cull/finalize + indirect-TRIANGLES draw. The SHADER half stays authoring-time codegen (TerrainChunkVertexSource splices into the ptex3d material's generated fxv2 — rides shaderpath, model B). Wire: the chunk drawable serializes INLINE on the SG node; `materializeAndWireScene` resolves its manifest+material by name. Authoring: `assets.HeightField.build()` is now LAZY like Hypermesh (no ctx → bake DEFERS to load — the C++ materializer bakes cook-cached at first play; tojson does zero GPU work). Scene `ren_terrain.py` (ork.data/scenes): voronoi 512² @ 512m/60m, chunk 128. VERIFIED: bake-at-load in pure C++ (height.exr written by the host), chunk drawable materialized (4×4 chunks, 1.0MB SSBO), full FPS, AND the 1.7 live round-trip BYTE-IDENTICAL (11,507 bytes) with the terrain re-materializing from the clone. **All three families — particles, hypermesh, terrain — now author→serialize→play→live-round-trip in `ork.ecs.player.exe` with zero Python.** | 1.7 → 2.14 | M+M | Goal C stops being aspirational here. |

---

## Stage E — features that now fall into place (order within E is by want, not need)

All of these land on contracts/infrastructure already locked above — no rework risk, mostly additive:

- **E.1 DisplaceByField** (2.3) — **LANDED 2026-06-11.** The first real cross-family graph edge runs: terrain modules (T.fbm/voronoi/terrace + the whole TerrainNode algebra) and hypermesh modules compose in ONE dflow graph; `DisplaceByFieldData` (hmdflow_module_displace.cpp) consumes mesh-In + the B.3 HfImage interchange plug and bilinear-displaces positions by the terrain planar convention (uv = P.xz/extent + 0.5; mode = along-normal | world-Y; amount/extent runtime plugs; reflected day one). **ENV-OWNERSHIP PRINCIPLE (owner-ratified, the load-bearing design call):** the module that BRINGS a family into a graph carries that family's parameters and stocks its env — displace's reflected `field_dim` + its onLink stocks the terrain BakeEnv on the GraphInst (TypeKeyedVars, coexists with MeshEnv; link()-before-activate() guarantees the generators alloc at the right dim). The generic mesh drivers know NOTHING about fields — no per-module special cases in driver code, ever. Matching self-defense: BakeEnv grew driver-capability flags (`_per_op_sync`, `_flushes_captures` — only bakeHeightfield sets them); the CPU fill modules + Capture sinks assert the capability they need instead of mis-running in a single-dispatch-phase host. Python: `Hypermesh.__init__` opens the family trace context (closed at generatedflow/materialize/save), so `T.*` calls inside a mesh composition emit into the mesh graph; `m.displace(src, field=<TerrainNode>, amount=, extent=, mode=, field_dim=)`. GATE `test_hypermesh_displace.py`: const-field EXACT oracle, gradient closed-form mapping oracle (proves field_dim honored — dim enters the formula), fbm smoke, and the mixed-family graph round-trip (byte-identical serdes + byte-identical clone re-bake + clone live-materializes). Demo asset `assets/hypermesh/displace_demo.py` (fbm→terrace algebra→displace→smooth_normals; clone GPU-bakes 37k verts headless). Full 25-gate hypermesh/terrain/particles sweep green. KNOWN v1 LIMITS (deliberate): terrain generators re-dispatch every live eval (no field dirty gate yet — cheap at field dims); one field resolution per graph (first displace stocks the env; mismatch warns loudly); CPU fill modules / captures excluded from mesh graphs BY the capability assert (bake those with a HeightField asset). *Landed on B.1/B.2/B.3 exactly as planned — zero rework of the substrate.* **+E.1b REALTIME FIELD PARAMS (same day, owner-directed: "inch field graphs toward realtime update" for future live editor control):** (1) `dflowgfx::IPrePhaseParams` (interchange.h) — the FAMILY-NEUTRAL pre-dispatch-phase host-write hook; MeshComputeInst + TerrainComputeInst both inherit it; ALL drivers (mesh live hook, mesh recompute/bake, the terrain bake's both paths) call it uniformly. (2) BakeEnv grew the B.4 clock (`_abstime`/`_dt`, SAME contract as MeshEnv); the live render hook mirrors ONE clock into every family env (pause/resume hold both). (3) fbm + noise CONVERTED to runtime params (the live-editor recipe, repeatable per module): only STRUCTURE bakes into shader text (dims/octave bound/basis/warp presence); freq/amp/offset/warp_amt ride a params SSBO RE-READ FROM THE DATA PLUGS every pre-phase (the inset/extrude pokeable-channel idiom — m.inputs.* edits land next frame, no recompile); cook-hash salts bumped (fbm v4 / noise v3). (4) `offset_vel` (vec2 cells/sec plug, fbm + all four noise bases): effective offset = offset + vel·abstime — declarative serialized field animation, zero per-frame Python; T.* ops auto-mark the composing Hypermesh animated (trace-guarded against the stale build-asset binding). (5) The conversion is NUMERICALLY TRANSPARENT (gate values byte-identical pre/post). GATE +2 cases: animated field scrolls live / pause holds / resume works (flat grid → only the FIELD can move geometry), and an animated graph's BAKE is the deterministic t=0 snapshot (two bakes byte-identical, velocity ignored). FIXED en route: DisplaceByField read inst-plug copies (pokes silently dead) → data plugs. demo displace_demo now ROLLS (two counter-panning fbm layers). 25-gate sweep green. POST-LANDING FIX: ork.hypermesh.viewer.py hand-rolled its animated check TWICE (onUpdate/_time_param at load; onUpdate-only on live-reload) → animated fields rendered static in the viewer while the gate (via drawable_data → is_animated) passed; both sites now use `asset.is_animated` — the base-class property is the SINGLE SOURCE OF TRUTH for "needs per-frame re-eval". NEXT INCHES (the recipe, module by module as needed): remap/combine/terrace scalars → params SSBO; the field dirty gate (skip re-dispatch when params+time unchanged).
- **E.2 InstanceSet plug + ScatterSource module + instance-sink** (2.4 + 2.10) — **LANDED 2026-06-11.** (a) **The C++ scatter placer** (`hfdflow_scatter.{h,cpp}`, a 1:1 port of scatter.place) hooked into `HeightFieldGenData::materialize` post-bake — every reflected ScatterSinkData places to `<assetcache>/terrain/<asset>/<sink>.ogeo` at load, ZERO Python. **DETERMINISM SPEC (language-independent):** all draws come from a STATELESS counter-based hash RNG `hash(seed, cell, stream)` (splitmix64 chain, 8 named streams; subsample = keep-smallest-SS_PRIO, output in cell order) — replaces numpy RandomState in scatter.py too (now the PARITY REFERENCE; `_run_scatters` calls the C++ placer via pyext — ONE implementation). PARITY GATE `test_terrain_scatter_parity.py`: counts/type_id/variant_seed EXACT, P bit-identical, xform ≤ 7.6e-6 (libm rotation noise; the C++ matches numpy's float32-then-lift rounding order), count-mode cap exact, C++ byte-deterministic. (b) **InstanceSet** = the SECOND interchange GPU-resource handle (interchange.h): matrices SSBO + TYPED attrs SSBO (vec4: x=type_id, y=seed01 — **the matrix-bottom-row smuggle is RETIRED**; the instanced VS now always reads `storage_inst_attr[gl_InstanceIndex]` → frg_clr; the legacy `instances=` floats path converts bottom rows → attrs on upload, so old assets render identically) + count + version. (c) **ScatterSource module** (hypermesh family): loads the .ogeo once at onActivate (by `ogeo_path` or the PORTABLE `scatter_asset`+`sink` assetcache convention), type_id-filters, fills the set; missing artifact = LOUD assert (declaration order = dependency order). `LiveHypermesh._instances` discovery + `setupMeshRender` binds the graph's set (returns {faceid, instMtx, instAttr}). (d) **DSL:** `m.instance_source(scatter_asset=, sink=, type_id=)` (replace semantics — one set per graph); make_drawable AUTO-instances from the graph's set; **`install_hypermeshes` is retired to the one-wire loop** (same signature, numpy shard/smuggle glue deleted). GATE `test_hypermesh_instedge.py`: bake→place→typed-edge materialize (count exact) → scenegraph render → MIXED-graph byte-identical round-trip + clone re-instances → install_hypermeshes over the edge. 28-gate sweep green. **DEMO `ren_scatter.py`** (ork.data/scenes; terrain DSL assets/terrain/scatterhills.py): terrain + two-type scatter + two instanced rock hypermesh entities (Boulder valleys / Shard ridges) — VERIFIED in ork.ecs.player.exe zero-Python: bake at load → C++ placer (1024 points) → both ScatterSources type-filter (513/511) → terrain chunks + two single-indirect-draw instanced nodes at 120 FPS. NOT in scope (deliberate): dynamic/streamed sets (the _version field is the hook), GPU cull for instanced hypermesh (E.4), InstanceFilter graph op (3.1). **+E.2-WALK (owner-directed, 2026-06-11): WALK-ON-TERRAIN as a reusable library.** (a) `BulletShapeTerrainData.hf_asset` — the asset-wired heightfield collider: resolves the baked height EXR + manifest scale at shape creation, so bullet collides with EXACTLY what the chunks render (the entity sits at y = 0.5·height_m — the baked channel is auto-exposed [0,1] exactly, so bullet's centered AABB lands right). (b) **CharacterControllerComponent/System** (ecs/physics/CharacterController.{h,cpp}) — the FPS-example recipe as DATA: pairs with a sibling capsule BulletObjectComponent (angularFactor (0,1,0) upright lock + a declared "walkforce" DirectionalForce); **INPUT ENTERS THE ECS AS CONTROLLER MESSAGES** (owner-ratified: `systemNotify(CharacterControllerSystem, InputKey, {key:int, down:int})` — host-agnostic; a future opt-in PythonSystem can route the same stream; NO python in the player otherwise). Per update: heading yaw (A/D), walk force along heading (W/S), speed clamp, jump impulse near-rest (SPACE), camera PUBLISH (fps/follow) via the standard UpdateCamera message + 2s pose telemetry on ecs.charctl. (c) **Player walk mode**: detected from the scene's CharacterControllerSystemData — forwards plain KEY_DOWN/KEY_UP as InputKey messages, the EzUiCam YIELDS (the character owns the camera), pause moves Space→P (Space = jump), `--autowalk N` scripts W through the same message channel (the gate's lever). (d) **Scene-DSL library**: `self.terrain_collider(hf_asset)` + `self.walker(spawn=, radius=, height=, move_force=, jump_impulse=, cam_distance=...)` — two calls give ANY scene walking; all dims/drive/camera reflected. ren_scatter carries both. GATE `test_ecs_walk.py` (transient player window): WALK MODE detect, settle, autowalk displacement, re-settle, **ALIGNMENT PROBE: rest y within 0.9m of the baked-EXR height + capsule offset** — physics and render consume one artifact, verified empirically. pyext: BulletShapeTerrainData + CharacterControllerComponentData bindings (snake_case props). **+PYTHONSYSTEM INPUT ROUTING (owner-directed, 2026-06-11): the keymap is SCENE DATA.** Two-hop input: the host forwards RAW InputKey {key,down} to the scene's PYTHONSYSTEM; its SCRIPT (walker() ships scene/walk_input_system.py — W/S move, A/D strafe, cursor L/R turn, cursor U/D pitch, SPACE jump) translates user actions into SEMANTIC CharacterControllerSystem messages (MoveInput{x,z} / TurnInput{rate} / PitchInput{rate} / Jump) — the C++ system knows NOTHING about keys; raw-key handling was REMOVED (one path). The player embeds python OPT-IN ONLY (peeks the .ecs for PythonSystemData before threads; zero-python scenes never init the interpreter). PythonSystemData's script path is now REFLECTED ("ScriptPath" — describeX was EMPTY; serialized scenes silently lost the script). **TWO EMBEDDING BUGS found via a MINIMAL pysys probe + lldbserve (isolate before debugging the full stack — owner's call, correct):** (1) _reload's toAbsolute() drops the leading slash of absolute script paths (the recorded toAbsolute trap) → the one SILENT branch: script "not found", hooks unbound, keys mutely dropped at full FPS — now bypassed for absolute paths + missing declared script hard-asserts; (2) **the embed GIL deadlock**: Py_InitializeEx leaves the embedding thread holding the GIL forever → the update thread's Py_NewInterpreter blocks eternally (app renders at 120 FPS, script never runs) — an embedding host must PyEval_SaveThread() after init. (Also: console silence through redirected stdout can be FULL BUFFERING, not a hang — trust file-evidence markers.) PythonSystem verified ALIVE on 3.14t embedded (probe: init/link/activate/1200 updates in 12s). Walk gate green THROUGH THE SCRIPT CHAIN (41.4m, rest ±0.91m); demo camera = first person pivoting at the capsule top (eye_height = extent/2+radius, cam_distance 0). 28-gate sweep green. **+SCATTER COLLIDERS + SCRIPT COLLISION INTERCEPTION (owner-directed):** the scatter() sink declares per-type physics proxies (`colliders={type: ("sphere",r)|("capsule",r,h)|("box",x,y,z)}`, reflected on ScatterSinkData); BOTH placers bake them PER POINT into the ScatterSet (proxy_kind/proxy_dims channels — the collider shape rides the DATA, parity-pinned); `BulletShapeScatterData` (asset-wired like ScatterSource) builds PER-ITEM STATIC RIGID BODIES — bullet's many-instances idiom: ONE shared base convex per (kind, dims), each item a tiny btUniformScalingShape wrapper (per-body scale can't live on a shared shape) + rigid transform; bodies parked on the shape inst (ShapeBatchBodies) and removed at component deactivate; the component's own body = btEmptyShape anchor (kind -1 = no body; entity at ORIGIN — item xforms are world-absolute). The single-btCompoundShape first cut is RETIRED — see the static-scale lessons below. `BulletObjectComponentData._notifyCollisions` forwards contacts to the PythonSystem as "Collision" notifies ({nameA,nameB,pointA/B,normalOnB,entrefs,groups}) — the system SCRIPT intercepts (walker sets it; walk_input_system.py has the branch). **THE STATIC-SCALE SAGA (four stacked traps, diagnosed in order; the cure for guessing was wall-clock telemetry — `bullet timing: step/scan ms/s` printed every 2s):** (1) per-manifold python notifies: a RESTING body contacts EVERY tick (480 UPS), each crossing into the sub-interpreter (GIL bind/call) — notify CONTACT-ENTER transitions only (per other-entity, 0.25s re-arm); continuous-contact data belongs to polled/throttled queries, never per-manifold events. (2) `contactTest` per tick: against a compound it constructs a collision algorithm that PREALLOCATES one child algorithm PER CHILD → O(density)/tick — replaced with the MANIFOLD SCAN (stepSimulation already computed the persistent manifolds; reading them is O(actual contacts); never query what the step computed). (3) `m_forceUpdateAllAabbs` DEFAULTS TRUE: every substep recomputes EVERY object's AABB into the dbvt (~4M updates/s at 8.4k statics) → `setForceUpdateAllAabbs(false)`. (4) THE BIG ONE — ACTIVE STATICS: the activate path called `activate()`+WANTS_DEACTIVATION on every body, statics never leave that state, and ONE active partner forces narrowphase on a pair each substep; the world-spanning heightfield AABB pairs with ALL 8.4k rocks → ~4M convex-vs-heightfield dispatches/s = step pinned at 1000ms/s (the owner's 4k-dynamic-spheres-ran-fine comparison was the tell: their cost collapsed when they SLEPT). Fixes: static non-kinematic bodies get ISLAND_SLEEPING (never activate()), and a broadphase `btOverlapFilterCallback` REJECTS static↔static pairs at creation (bullet can never use one). Result: step 1000→~40-65ms/s, 120 FPS at 8467 objects, collisions still reporting. Also: cross-system notify uses the PUBLIC `System::_notify` (the _onNotify base decl is protected; SGSystem only allows the direct call because its overrides are public). RUNTIME TUNING: CharacterControllerSystem `SetParams` message (any subset of move_force/max_speed/brake/turn_rate/jump_impulse) — the input script owns the FEEL (PARAMS dict in walk_input_system.py); asymmetric drag (`Brake` reflected: hard exp brake on input release, ZERO drag while driving, bullet linearDamping ~0); buffered jump (0.25s TTL + |vy|<0.75 near-rest). Scene at REALISTIC scale: 2048m/2048², 100m navigable rolling relief (terracing removed — risers were cliffs), 2.0m capsule, Ground ptex3d material (two band-limited fbm_aa bands: 64m..4m color patches + 4m..6cm brightness detail = the 10mph optical-flow reference); player passes -f/--ssaa through (viewer forwards).
- **E2A "shootable balls" (owner-directed, 2026-06-12) — PROJECTILE POOLS (dynamic name-paired instancing) + the two render-path bugs they exposed. LANDED E2A1..E2A4.** The feature: '/' in the walk scene queries CharacterControllerSystem for the camera ray (the NEW synchronous `system.request` path — `_request` is public like `_notify`; system-script context = update thread, direct virtual call) and spawns a physics ball through a link-time-resolved spawner handle (`simulation.findSpawner` → `Spawner.spawn(pos=, vel=, avel=, scale=)` — `Simulation::spawnDynamic` registers a controller ref like the command path). VISUAL = the serializable instanced-node pairing: BOTH components carry `instance_node_name` (reflected `InstanceNodeName`; the pyext-only `_INSTANCEDATA` ptr-equality form still resolves through its groupname) naming a SYSTEM-LEVEL `InstancedModelDrawableData` node (`SceneGraphSystemData::_nodedatas` + `MultiLayers` + `maxinstances` all newly reflected — system-level nodes now round-trip). **E2A1:** `SpawnData::_autospawn` was UNREFLECTED → deserialized `autospawn=False` spawners GHOST-SPAWNED one entity at scene start (an invisible ball the walker stood on — its collision print was the tell). **E2A2 (the render bugs — found via the NEW oracle `pyext/tests/renderer/instancing/instanced_model.py`, the FIRST coverage of `FWD_CT_NM_RI_IN_MO` in the modern forward path; instancing.py covers the rigid-primitive drawable and the hypermesh demos FWD_SSBO_CUSTOM — the instanced MODEL drawable was an orphaned path):** (1) `IRenderer::_renderCallbackRenderable` static_cast EVERY CallbackRenderable's drawable to CallbackDrawable to read `_renderEnabled` — type-confused garbage for InstancedModelDrawable (the only non-CallbackDrawable that sets `renderable._drawable`), silently dropping ~every draw (the player showed ONE draw in 20s). `_renderEnabled` moved to the Drawable BASE; the check is type-correct for all riders. (2) `storage_instancing` (stdtools.i2) had silently DIVERGED from `InstancedDrawable::k_max_instances` (1Mi vs 128Ki — the "TEMP flora runtime-array" experiment): the fixed array sizes ARE the std430 layout contract, so colors/pickids reads landed past the bound buffer → instance_color = 0 → BLACK balls. Both sides agree again (131072) and both now carry change-both-together comments. ALSO: unreadable shader imports FAIL EARLY (OrkAssert in the cache-hash expansion — a silently-skipped import is invisible to the shader-cache content hash = stale-chunk debugging trap); `ORKID_DISABLE_SHADER_CACHE=1` = the recompile-every-run knob; deep-import cache invalidation verified working (level-3 includes hash correctly); the per-draw forward-env-bind-vs-DPP-program "not found in merged resources" spam is now logged once per (program,param). **E2A3 (lifecycle):** ONE velocity contract — BulletObjectComponent activate consumes entity-varmap `initialVelocity` (the FSM scheduled-spawn writers were WRITE-ONLY dead code until this consume) + the `initialAngularVelocity` twin (spawn `avel=` — topspin about cross(up,dir) that converts to forward drive on frictional contact; the shoot script overspins 2x at 240 rad/s); `spawnDynamic` stamps `_despawnTime` from the spawner's reflected Lifetime (scene `lifetime=8` → declarative recycling; `_updateEntityLifetimes` reaps; despawn → SG unstage frees the slot); `freeInstance` restores the IDLE-SLOT CONTRACT (zero-basis = invisible — count==capacity slots always draw, so a freed slot left posed = a frozen ghost); `allocInstance` fail-soft (-1 + loud log; SG stage skips the visual — a gameplay-rate event never aborts the host); bullet wires the motion state IMMEDIATELY when the SG `_INSTANCE` already exists (static-spawn ordering hole: the stage render-op can drain BEFORE activate installs the callback — cb<0> in the field logs). CONTACT-COMBINE NOTE (bullet multiplies friction/restitution): static colliders (terrain/scatter) now carry 1.0 so each dynamic body's own values read as its effective response; the walker keeps its own zeros — walk feel unchanged. **E2A4 (surface):** `SG.instanced_node(name, model=, capacity=, layers=)` first-class wrapper verb (the scene-side `_decl.sub_calls` reach-in is gone) + `Scene.projectile_pool(name, model=, radius=, mass=, friction=, restitution=, max_count=, lifetime=)` library call (the walker() pattern — data here, SHOOT policy in the input script: key/speed/offset/spin in walk_input_system.py SHOOT_*); new pyext props born snake_case (`instance_node_name`); scatter colliders grew `("cone", r, h)` (shared btConeShape + uniform-scaling wrapper, same batching idiom; BASE-AT-ITEM-ORIGIN convention matching the hypermesh cone primitive — the Shard's exact proxy, body origin lifted +h/2·scale along item-local up); the walk gate grew the SHOOT case (ORK_WALK_SELFTEST selftest spawn + no-ghost regression assert + alive-past-despawn telemetry count) and now reads HEIGHT_M/EXTENT_M from the scene SOURCE (its duplicated constants had silently staled when the scene rescaled 512/60 → 2048/250). NOT in scope (deliberate): converging this with E.2's InstanceSet (a bullet-driven DYNAMIC InstanceSet would let a hypermesh asset be the projectile visual — the `_version` hook is the entry point when wanted).**
- **E2B "fire trails" (owner-directed, 2026-06-12) — PER-PROJECTILE WORLD-SPACE FIRE+SMOKE + the 10x pass. LANDED (heat-distortion postfx = the open bonus).** The feature: `projectile_pool(trail=<ParticleSystem asset>, trail_delay=)` puts a ParticlesComponent on the projectile archetype — ONE shared graphdata serves every spawned ball via the RESERVED **"@host" entity key** (`GraphInst::_resolveEntityXf` resolves it to the HOSTING entity per slot — the per-instance binding a published name cannot express). World-space contract: particles live where emitted (sgnode stays at IDENTITY — composing the host xf would double-transform), only the emit Offset follows the entity (`E.entity("@host").transformPoint(...)`); buoyancy (DirectionalForce up) + drag = heat-rise terminal drift. **TRAPS EN ROUTE:** EntMotionState's instanced branch never wrote the ENTITY transform back (particles tracked nothing — the entity xf is the universal live-pose source; instanced visuals don't exempt it); the VK descriptor-set cache keyed only on samplersHash → every slot reused slot 1's particle SSBO descriptor ("only one fireball") — bound SSBO buffers are now in the cache key; multi-renderer graphs needed `Context::setRenderLambda(key, order, lambda)` (registry, mutex — update-thread link races render-thread iteration) + reflected `draw_order`; ParticlesComponent._onLink ASSERTS on missing ParticlesGlobalSystem (dynamic-spawn archetypes never trip composition auto-register; fail early); delayed start at BOTH scopes (component `StartDelay` reflected → slot_time runs from -delay, update skips while negative; per-emitter StartDelay plug). **THE 10x PASS (one draw, not two chains):** `FreestyleParticleMaterial` — flipbook cookie (texgrid recipe) × gradient ramp over unit_age in ONE fragment (`ps_freestyle_ptc`), `tfreestyleparticle_{sprites,streaks}` pair (geometry-agnostic, one FS); **PREMA-authored** (rgb adds, α occludes): ramp w≈0 = additive flame → w>0 = absorptive smoke → 0 = gone, so each particle IS the fire that becomes its own smoke (animated cookie covers both phases; no draw-order fight, no popping). Fully reflected day one (gradient/texture-asset/griddim/color/blendmode/depthtest/factors) + **`shader_path`** — swaps the whole fxv2 (must provide the technique pair): the hook where DSL-generated particle shaders and the heat-buffer variant land. fireball.py = ONE chain. **Layer fix (fire occludes ball):** trail ParticlesComponents land on `std_transparent` (draws after std_forward; depth-tests against the prepass so geometry still occludes correctly) — DSL default + projectile_pool explicit. **Role-layer autodeclare (owner-directed, keyed on the RENDERER):** `RenderCompositingNode::renderedLayerRoles()` virtual; `pbr::ForwardNode` returns the canonical role list (single source — `_render_top` assembles the frame from the same fn); `Scene::initWithParams` pre-creates each role as a real layer (idempotent, empty = free) — kills the under-declared-scene bug class (findLayer asserts / content parked on unrendered sg_default) without giving deferred scenes forward layers. **Orphan-drain (owner ask): particles OUTLIVE their despawned host.** `_onUnstageComponent` transfers any slot with live particles to `ParticlesGlobalSystem::_orphans` — emission inhibited, sgnode stays, and the "@host" resolver REBOUND to a frozen `DecompTransform` snapshot (`decompose(composed())` flattens parents) — THE TRAP: the captured Entity is reaped after unstage but emitter Offset plugs evaluate every tick (deref = SEGV seconds after first despawn). Orphans tick in the same compute-job list (parallel path included); released (render-thread detach + drop) when `lev2::particle::aliveCountOnParticleGraph(ginst)` (new helper: sums pool GetNumAlive across the graph) hits zero; system _onUnstage clears them at teardown. Verified: 40s rapid-fire (ORK_WALK_SELFTEST=2), 22 shots, ~14 despawns through the handoff, no SEGV, ~82 FPS; serialization round-trip carries all 9 material props (PREMA/gradient stops/asset-form texture). ITEM D LANDED (2026-06-12, owner-verified): **GENERIC AUX RENDER CHANNELS + PostFxNodeHeatDistort.** (a) `RenderCompositingNode::_auxChannels` (names; set from the scene param "AuxChannels" CSV in Scene::initWithParams BEFORE the role pre-create — each channel adds role/layer `aux_<name>` via renderedLayerRoles); the fwd colorpass renders each aux layer as an EXTRA PASS into a lazily-created RGBA16F RTG (primary dims, NO depth attachment) and publishes it into the CompositorDrawData properties under crc("aux_<name>") for postfx consumption — fully generic (glow masks/velocity/... reuse the same machinery). (b) Pass selection: the aux pass sets `RCFD->_subpassID="AUX"` + user-prop AUX_CHANNEL (crc); `MaterialBase::pipeline()` consults a per-material `_aux_teks` map (channel-crc → technique pair + OWN pipeline w/ additive no-Z rasterstate) and returns NULLPTR for non-participants — sprite/streak renderers SKIP on null (content opts in; over-attaching layers is free). ParticlesComponent `LayerName` is now CSV (node on first layer, added to the rest); the scene DSL auto-appends every declared aux layer to ParticlesComponents. (c) Fragment-DSL heat hook: `ctx.is_heat` trace flag (queried = opt-in; class traces 2 more times) emits `tfreestyleparticle_{sprites,streaks}_heat`; fireball's heat = cookie-shaped, AGE-GATED to the drifted plume (0.45–0.6 in, 0.75–1.0 out — the young flame core sits ON the projectile and depth-clipping there reads badly; Unity/Unreal's general fix = soft-particle DEPTH FADE, a follow-up needing NEAR_FAR in the fragment). (d) OCCLUSION: heat fragments depth-test MANUALLY against `RCFD_DEPTH_MAP` (the read-only prepass depth the color pass exposes for water) — gl_FragCoord.z vs sampled scene depth, discard behind terrain/solids (sampler2D ⇒ MSAA>1 primary unsupported; the aux RTG can't share the real depth attachment — the vk impl OWNS any `_depthBuffer` it sees and would clobber the primary's impl). (e) `PostFxNodeHeatDistort` (reflected strength/chroma/channel; registered + pyext-bound) refracts postfx_in by the heat-field gradient: SOFT-SATURATE the additive accumulation (1-e^-h) BEFORE differencing + HARD-CAP the offset (~24 texels) — unbounded stacked-sprite heat blew the gradient up and smeared distant background over the smoke; wide 12-texel differencing = broad shimmer lobes. Declared in ren_scatter via `scenegraph(aux_channels=["heat"], postfx=[("heatdistort", node)])` riding the EXISTING P3.D reflected postfx registry → zero new serialization machinery. TRAPS: RtGroup's 5th ctor arg is the USAGE selector (default "user"), NOT a debug name (unknown usage = vk switch assert); the role pre-creation + aux layers pushed DrawQueue::kmaxlayers past 8 → DOUBLED to 16 (owner call). **ITEM E LANDED (same day, owner-verified): the fireball EMISSION POINT LIGHT.** THE BUG (lights dead in ECS since ~May): `DrawableData::attachSGDrawable` — the hook that hands the scene's LightManager to the particles drawable impl — had ZERO call sites; the ECS path calls plain createDrawable() ⇒ `_LM` null ⇒ the emission light never registered (diagnosed by instrumenting buildLightingUBO: untextured_pointlights<0> forever; ORKID_DEBUG_LIGHTS=1 keeps the throttled dump). Fix: ParticlesGlobalSystem::_onStageComponent calls attachSGDrawable(slot.drawable, _sgsys->_scene). COLOR (the owner's "might need customization" concern): NO hook needed — PREMA makes the weighting implicit: each live particle's ramp sample weighted by `luminance × (1−occlusion)` (= its ADDITIVE-ness) ⇒ fire drives the light, smoke contributes nothing; serializable by construction (no lambdas). POSITION: the same weights yield a hot-particle CENTROID published as graphinst var `emission_center`; the drawable's light tracks it instead of the sgnode transform (identity for world-space trails ⇒ the light would sit at origin). Brightness fades in with burning mass (sum_w), × colorIntensity × the new REFLECTED `ParticleSystemGenData._emitter_{intensity,radius}` (wrapper kwargs → both materializers → ParticlesDrawableData; round-trips). CALIBRATION under the post-138a333 LINEAR falloff (atten = 1−d/radius; radius is LOAD-BEARING — the old inverse-square lit regardless of radius): working reference point_lights.py runs intensity 800/radius 150; fire trail shipped at 200/140. NOT in scope (follow-ups): soft-particle depth fade; aux half-res knob; shared-depth aux RTGs (vk impl-level feature). **+FRAGMENT DSL (owner ask, same day): the supporting shader DSL LANDED.** `ork.hypergraph.dflow.particles.fragment` — author the freestyle FRAGMENT as Python expressions over the ptex3d SurfNode IR (SAME SSA/CSE emitter + `P` op namespace — noise/fbm/voronoi flow through with their libsrcs/imports); ctx atoms = the fface_psys_grid contract (unit_age/random/aux/uv/modcolor/factors/griddim + ctx.cookie()/ctx.ramp() helpers); **`ctx.is_streak` is a TRACE-TIME bool** — the class instantiates twice and Python control flow unrolls into the two fragment shaders the technique pair binds (sprite/streak divergence with zero shader-side branching). `materialize_fragment(cls)` → content-addressed fxv2 providing `tfreestyleparticle_{sprites,streaks}`, persisted to `<staging>/dslshadercache/particles/` and referenced by the relocatable `<staging>` token (OWNER POLICY 2026-06-12, applies to ALL DSL codegen: generated shaders live in the per-family staging cache — ptex3d moved to `dslshadercache/ptex3d/` the same day; the B.5(c) `.shaders/`-next-to-asset form is RETIRED and the 55 committed artifacts removed — generated files never persist in the source tree; they regenerate at authoring time) — the reference rides the material's reflected `shader_path` and round-trips into the player. PREREQ REFACTOR: the SSBO vertex machinery (storage_particles + vfaces + vs_{sprite,streak}_ssbo) factored from particle_comshader.i2 into **particle_ssbo.i2** so generated files import it WITHOUT the stock techniques (duplicate-technique-name trap); particle.fxv2 imports common→ssbo→comshader (same concatenation order, zero stock change). TRAP: `inspect.getsourcefile` raises "built-in class" for spec_from_file_location-loaded DSL modules (resolve.load_dsl_class doesn't register sys.modules) — the persist-location probe reads `__init__.__code__.co_filename` instead. fireball.py's FireFrag = the stock ps_freestyle_ptc math authored in the DSL (+streak tail-fade exercising both traces) — owner-verified in ork.scene.viewer.py. v1 limits (deliberate): no ctx.param / extra ctx.tex (needs material-side generic binding); the heat-pass variant (is_heat_pass trace flag + aux-RT output) lands with item D.**
- **E.3 gid multi-material render bucketing (2.5) — LANDED 2026-06-12.** ONE hypermesh draws with N ptex3d materials in N indexed-indirect draws. (a) **`GidAssign`** — THE ONLY verb writing the LOCKED gid band (__tags[20:32), A1 contract; every other tag write was ALREADY hard-masked and `S.gid` already existed — the review's "no contract enforcement" row was stale): mesh passthrough, runtime {gid, slot} (slot=select bit, -1=ALL faces — CREATES the channel on an untagged mesh, so fresh primitives partition without a select); DSL `m.assign_gid(src, gid, slot=None)` fills the reserved stake. (b) **Bucketed triangulator** (hmdflow_render): the args buffer is now an ARRAY of 4096 VkDrawIndexedIndirectCommand slots (one per gid; offset = gid*20); cs_reset(4096 threads) → cs_count (per-face tri-index histogram) → cs_scan (1-thread serial 4096-bin exclusive prefix → per-slot {indexCount, firstIndex} + cursors; dirty-frames only) → cs_tri scatters each face's fan into its bucket's contiguous range. **UNBOUND gids FOLD TO SLOT 0** at count+scatter time (host-written 128-uint bound-gid bitmask) — an assign_gid without a material binding renders with the DEFAULT material instead of vanishing (ops self-defend); tagless meshes = everything in slot 0 = the classic single-draw shape (regression gates byte-neutral). (c) **N-material draws**: `ComputeDrawable::BucketDraw` {material, argsOffset, OWN graphicsStorage} — storage BLOCK handles are PER-SHADER (each ptex3d's generated fxv2 has its own), so buckets carry their own lists; the live refresh updates each bucket's first-5 (vertex channels) BY INDEX, same contract as the main draw. `HypermeshDrawableData` grew reflected `gid_materials` (gid-string → material asset name) + the generic `_material_resolver_named` (ECS installs the registry lookup; the build WAITS until the full material set resolves — no flicker-in). Author surface: `drawable_data(material=default, materials={gid: wrapper})`. (d) GATE `test_hypermesh_gid.py` (assign-all + channel creation / slot-assign + masked-write survival / byte-identical serdes + clone tag parity / drawabledata round-trip) + 5 core regression gates green. DEMOS: `ren_gid.py` (one icosphere: chrome band + 2 cap materials) and ren_scatter's Shard (gid-1 polished tips — buckets × INSTANCING: instanceCount rides every slot's command). FOUND EN ROUTE: a fresh-channel zero guard firing on upstream-aliased channels (std::vector(-1) → length_error("vector") surfacing as pybind ValueError) — zero only what you created; gate harnesses must NOT `except BaseException` around a body that calls sys.exit (SystemExit → double-teardown SEGV after PASS). v1 LIMITS (deliberate): gid changes re-bucket on the next TOPOLOGY rebuild, not per frame (animated gid swaps need a dirty hook — none of the racer's material zones animate); the 4096-bin scan is serial 1-thread (dirty-frames only, µs at that size; parallelize if profiling ever cares); python-viewer make_drawable materials= parity not wired (ECS path only).
- **E.4 instanced-hypermesh completion — LANDED 2026-06-12 (cull verified live: 12,074 instances → 5–700 visible per view, 94–99% rejected).** (a) **Instanced depth-prepass**: the generated ptex3d fxv2 grew `FWD_SSBO_CUSTOM_INSTANCED_DEPTHPREPASS` (instanced pull-VS × depth-only FS, same position math as color — no z-fight; codegen v19); `_createFxPipelineDPP` selects it for ssbo+instanced BEFORE the non-instanced ssbo branch (which would draw every instance at identity); materials without the variant fall through to no-dpp (today's behavior). HypermeshComponent attaches its node to the `depth_prepass` layer alongside its color layer (the SceneGraphSystem multi-layer pattern) — hypermesh finally has early-z; in dpp the main+bucket draws each self-select their depth pipelines (a material lacking one yields null and self-skips). (b) **Per-view GPU instance cull** (`MeshInstCull`, hmdflow_render): the proven cs_cull_mtxonly sphere-vs-frustum, extended to compact matrices AND the E.2 typed attrs TOGETHER (per-instance data can't desync) + a fanout pass writing the visible count into EVERY bound gid slot's indirect instanceCount (cull × E.3 buckets compose by construction). Runs via the new generic `ComputeDrawable::_perViewCompute` hook (onPreRender per VP, own dispatch phase, camera handed in directly — no block plumbing); graphics pipes bind the CULLED buffers. BOUND: auto-computed at bootstrap from a one-time position readback (+5% pad) of the materialized mesh; reflected `cull_bound` override for meshes that animate beyond static bounds; `cull` reflected, author surface `drawable_data(cull=True)`; w<=0 = LOUD disable. ORKID_DEBUG_CULL=1 = throttled count/visible/bound readback. **THE BUG THAT SHIPPED INVISIBLE ROCKS (and the lesson): the hm_drawable bootstrap grafts cdd fields onto the live drawable MANUALLY (it does NOT call createDrawable) — `_perViewCompute` wasn't in the graft list, so the cull never ran while the pipes were already bound to the never-written culled buffers.** Any new ComputeDrawable/Data field MUST be added in BOTH copy sites (createDrawable + the hm_drawable graft) — second site is easy to miss; diagnosed by bisect (cull=False scene flip, no rebuild) + the env-gated readback. (c) **First `frg_clr` consumer**: `Solid(instance_variation=v)` scales albedo by (1−v..1+v) from the E.2 per-instance seed01 (Cd.y) — placed copies stop looking cloned; v=0 default keeps generated shaders byte-identical. ren_scatter: both rock types cull=True, boulders instance_variation=0.35. NOT in scope (deliberate): dynamic instance matrices (the InstanceSet `_version` hook — no dynamic sets exist yet); occlusion culling (the dpp depth output is the prerequisite, now in place). (2.8, 2.9, 2.16)
- **E.5 triangulation floor (2.13) — LANDED 2026-06-12.** `cs_tri` ear-clips on the Newell best-fit plane: tris emit directly; quads + ngons (n<=60) project to the plane basis and clip ears (convexity = cross-sign vs the projected ring's orientation; ear test = no remaining vertex inside; RING-ORDER emission preserves the polygon's winding); degenerate normal / n>60 / no-ear (self-intersecting input) fall back to the classic corner-0 fan of the REMAINDER — output is exactly (n-2) triangles in EVERY path, so the E.3 bucket histograms and index capacity are method-agnostic (the count kernel never changed). Positions joined the shared triangulator interface (slot 11). GATE `test_hypermesh_triangulation.py` — `hypermeshTriangulationSelfTest`: hand-built GpuMeshes (convex quad / DART quad / L-hexagon / comb octagon) through the REAL MeshRenderTri dispatch + index readback + the AREA ORACLE (per-tri orientation must match the polygon — a folded fan emits flipped tris — and summed tri area must equal the analytic polygon area — a folded fan double-covers); all four exact (4.0/1.9/3.0/7.0). 4 render gates green (behavior-neutral for convex content). TRAP: **`pass` is a shadlang KEYWORD** (technique pass blocks) — as a shader-text loop variable it breaks the parse with a misleading token error pointing at the following `++` (statement-level `++` itself IS grammatical: ExpressionStatement -> postfix tail). **SAME-DAY BONUS (the mtl_lilies player crash): the C++ `MeshGenData` materializer LANDED** — `materializeMeshGen` (asset_gen_vdb.cpp) reads the .ogeo sidecar back (Geometry::readChunkfile -> submesh mergeVertex/mergePoly -> rigidprim fromSubMesh; MicroMesh is pyext-ONLY, hence the submesh path), material BY NAME; materializeAll dispatches it (baked-geometry meshes now play zero-Python — lily_sphere 10242 pts / 20480 polys @ ~118 FPS); `DrawableCache::fetch` now guards a null `createDrawable()` LOUDLY instead of crashing the host on any future unmaterialized gen type.
- **E.6** — the **C++ TerrainChunkDrawableData half LANDED EARLY** (2026-06-11, pulled into D.5 to close it — see the D.5 row). **2.12 LANDED 2026-06-12 — material rebind-propagation + MaterialParamSink.** (a) THE REBIND CONTRACT: `GfxMaterial::bindParam` bumps `_bound_params_stamp`; `FxPipeline::_syncMaterialParams` (top of beginBlock) re-overlays the material's CURRENT `_bound_params` when its seen-stamp lags — O(1) compare when clean, so a material-level rebind made AFTER pipelines were cached is live at every pipeline's next draw (color, depth-prepass, gid buckets — all of them), no per-draw Python closures. `FxPipelineCacheImpl::_on_miss` now sets `_material_ptr` GENERICALLY (FreestyleMaterial's creator never did — material-level binds were a complete no-op for the whole generated-ptex3d family before this). The python `PBRMaterial.bindParam(name, value)` docstring's claim ("propagate into every pipeline") is now actually true. TRAP DISCOVERED: **`FreestyleMaterial::bindParam` SHADOWS the base** with an immediate-FXI direct-draw bind (null-target crash if uninitialized) — `pbrmaterial_ptr_t` callers get the base deferred contract (PBRMaterial does NOT shadow); freestyle-typed callers do NOT. GATE `test_fxpipeline_rebind.py` -> `fxPipelineRebindSelfTest()` (no GPU): pre-creation bind / post-creation REBIND / new param / clean-stamp O(1) skip / re-arm — 5/5. (b) **MaterialParamSink** (`hmdflow_module_paramsink.cpp` + DSL `material_param(src, name=, value=)`): drives a material UBO param BY NAME (a ptex3d `ctx.param`) from the graph. Mesh PASSTHROUGH module (chain position ergonomic only); the float `value` plug is a pokeable DATA plug. The DRAWABLE drains it per-frame — C++ `hm_drawable` (build-time: resolve param name against main + every gid-bucket material, LOUD log when it matches nothing; per-frame: read the DATA plug — NOT an inst copy, so a poke is live even on a STATIC graph that never recomputes — bindParam-on-change) and the python viewer `_onUpdate` (parity drain via the NEW generic `InPlugData.value` pyext getter). GATE `test_hypermesh_paramsink.py`: passthrough count+tag parity / poke-read (fp32) / byte-identical serdes + clone recompute parity. DEMO `param_pulse.py` (ork.hypermesh.viewer.py param_pulse): STATIC icosphere whose emissive pulses via the sink — zero geometry recompute. (c) 2.20 rider: the **`bindParam(FxShaderParam-handle, value)` python overload RESTORED** (pyext_gfx_pbr) alongside the ptex3d-only name:str form — the standing regression memory closes. **2.19 LANDED 2026-06-12 — hypermesh cook-cache participation.** STATIC graphs (DSL sets `graphdata.cacheable = not is_animated` at `_close_trace`) join the per-node disk cook cache. (a) IDENTITY IS CONTENT-ONLY: `hypermeshModuleIdentityHash` = the module's reflected-state JSON with the uuid envelopes STRIPPED (uuids are deliberate per-object identity for git history and survive deserialize — but the primary workflow re-authors graphs every run via scene.py→tojson, minting fresh uuids, so cook identity must derive from content alone). Model B makes the hash COMPLETE by construction (all authored state is reflected) — a new property can never silently dodge it. `MeshComputeInst::cookComputeHash` = global epoch + class name + per-class `_cookSalt()` (BUMP when an op's KERNEL changes — the hash can't see shader text) + identity hash + upstream Merkle. (b) STORE/LOAD are GENERIC on MeshComputeInst (one implementation, every mesh op participates): full output mesh (counts + semantic/named-vertex/face channels + vidx/face_offsets + the header BLOB) → DataBlock in the evictable `dflowcache` namespace; load re-acquires pooled channels + uploads + `markTopoChanged()`. SELF-DEFENSE: a channel whose pooled capacity can't cover the live count refuses to cache LOUDLY (mapping it OOB returned a null mapping → the original deref crash). (c) DRIVERS: unlike terrain, NO per-op submit+wait — loads are pre-phase host uploads, misses compute in the normal dispatch phases, and ALL stores defer to after the topology-readback cascade settles (post final submit+WAIT). The hypermesh drivers now NEVER call `GraphInst::compute` (its `_cacheable` branch is the core's SYNCHRONOUS cachedCompute — wrong for GPU graphs); `_computeSkipping` iterates insts directly (timing sink preserved) skipping the cook-loaded set; loaded nodes also skip writeParams + onTopologyReady. Foreign-family insts (terrain fields via DisplaceByField) always recompute (v1). GATE `test_hypermesh_cookcache.py` (unique-per-run salt defeats leftover disk state): COLD 3 computed+stored / WARM re-authored (FRESH uuids) 3/3 loaded with dump_obj BYTE-IDENTICAL geometry through the subdivide cascade / INVALIDATION one param → exactly that node + downstream recompute, upstream hits. ORGANIC ENGAGEMENT across the suite: the gid gate's two graphs SHARE their identical box node cross-graph, and its serdes CLONE loads 4/4 from cache with tag content asserted; instedge's clone restores 2/2 through the full instanced render path. **BUG FOUND+FIXED en route (the gate caught it): GidAssign's created `__tags` channel was LOST on re-eval** — writeParams overwrote the member with the (absent) input-channel lookup and the pow2-unchanged branch never re-acquired → eval 2 (any topology cascade) published NULL and silently no-opped the assign. Only chains with `assign_gid(slot<0)` UPSTREAM of a topology-readback op hit it (ren_scatter assigns AFTER subdivide). KNOWN LIMITATION (pre-existing, surfaced by the same gate): **subdivide does not propagate face `__tags` to children** — tags assigned upstream of subdivide are dropped; assign gids after it. **2.20 PARTIAL (2026-06-12):** (i) `bindParam(FxShaderParam-handle, value)` python overload RESTORED (see the 2.12 record). (ii) **SET_PARAM payload widening**: `ecs::decodeSetParamValue` (datatable.h) — tolerant numeric decode (float/double/int/uint32) replacing the asserting `get<float>` at BOTH ParticlesComponent SET_PARAM sites (the D.5 exact-typed-svar trap: python floats arrive as doubles, C++ literals as ints; non-numeric now logs + ignores instead of crashing the host). (iii) **HypermeshSystem grew SET_PARAM** (the 2.12 ECS leg): DataTable {name, value} pokes the "value" DATA plug of every MaterialParamSink whose param_name matches, on every hosted graph — gameplay drives a material UBO param by name in the zero-Python player (the drawable's per-frame drain picks the poke up with zero geometry recompute). TRAP: poking a plug from an ecs TU needs `<ork/dataflow/plug_data.inl>` (setValue is template-defined there — link error otherwise). (iv) **terrain round-trip STRENGTHENED to BAKE-EQUALITY**: test_terrain_asset.py now bakes the AUTHORED graph AND the deserialized clone and asserts the height EXRs BYTE-IDENTICAL (with this asset's capture cache=False the equality is genuine recompute determinism — the B.4 t=0 oracle — not cache replay). (v) **terrain↔material contract serdes LANDED (2026-06-12 — 2.20 CLOSES, E.6 ROW COMPLETE)**: `HeightFieldGenData` now owns the contract — reflected `material_asset` (the shading material's name) + `channel_samplers` (baked channel → sampler uniform map); the physical-params leg is the already-reflected dimension/extent_m/height_scale_m trio (carried by the .terrain.json manifest). `materializeAll` grew a POST-PASS: for each HF declaring the contract, resolve the material artifact by name and bind every channel's deterministic `<assetcache>/terrain/<name>/<channel>.exr` (via `HeightFieldGenData::channelPath`) to its sampler through the factored `PbrMaterialGenData::bindSamplerTexture` (the D.4 loop, now shared) — SINGLE SOURCE OF TRUTH on the terrain asset, no hand-synced path strings on the material; declaration order irrelevant (post-pass + the 2.12 live-rebind contract); missing material/param/file all LOUD. Authoring: `self.asset.HeightField(..., material="terra_mat", channel_samplers={"flow": "FlowMap"})`. GATE: test_terrain_asset.py asserts the contract survives serialize→deserialize byte-identically (plus the bake-equality from (iv)). (vi) **parallel_compute AUDIT (2.20 closes)**: the ParticlesGlobalSystem parallel block is CORRECT — incr-before-enqueue closes the early-zero race, decr-release/spin-acquire publishes compute writes, captures by value, per-job UpdateData; documented CONTRACT at the site: worker-side compute must never touch python/GIL, particle graphs must stay non-_cacheable (GraphInst::compute would take the SYNCHRONOUS cook branch), and the one KNOWN BENIGN exception to per-graphinst isolation is the E2B emission EMA's shared-material float writes (visual-only jitter under parallel slots); a throwing compute would spin the join forever (modules abort on assert instead).
- **E.7 Phase-3 generalization** (per the review): InstanceFilter, multi-sink, subgraph nesting, the sdfgrid family (gated on E.5), SceneWiring/SceneParameters (M5), the dirty-machinery implementation per the B.5(d) decision, Context-shared pool, ptex2d bring-up. **SDFGRID TRACK STARTED (2026-06-12; owner-ratified design): two-representation split — DENSE BRICK = the GPU-writable substrate (voxelize/CSG/MC), NANOVDB = the sparse read/transport blob (baked/track-scale; one buffer = cook DataBlock = sidecar = SSBO) — with nano↔dense bridges; voxelize sign = angle-weighted pseudonormal (exact on closed manifold; per-edge from MeshEdges, per-vertex from the adjacency CSR) + winding-number fallback on dirty input (boundary-edge probe is free) + flood-fill consistency pass; the shadlang NanoVDB reader is a HAND-PORT of the PNanoVDB read subset (~300-500 line .i2, 32-bit address mode); openvdb = CPU build/oracle ONLY (nanovdb sources exist at <staging>/builds/openvdb/nanovdb incl. PNanoVDB.h — headers not yet installed, obt flag needed at M3). Milestones M0 plug+brick+eval → M1 voxelize → M2 csg+MC (boolean chain) → M3 nanovdb → M4 cross-family+cook.** **M1 LANDED 2026-06-12 (the hard foundational primitive — GPU-native, gate-verified vs openvdb):** `MeshToSdf` (sdfdflow_module_meshtosdf.cpp) voxelizes a GpuMesh -> dense SDF brick. SIGN = angle-weighted PSEUDONORMAL (face Newell normal; per-edge pn from the MeshEdges f0/f1 table; per-vertex pn accumulated per-corner via fixed-point int atomics — only the DIRECTION matters for sign) with the WINDING-NUMBER fallback (solid-angle sum, |Σ|>2π inside) selected IN-KERNEL when the edge build counts boundary edges (the FREE probe: f1==~0u bumps an atomic), or forced via `sign_mode` (-1 AUTO / 0 pseudo / 1 winding). DISTANCE = per-voxel nearest feature over fan-decomposed tris with Ericson region classification mapped back to the POLYGON feature (fan diagonals classify as face-interior — exact for convex faces, the v1 contract). KERNELS: cs_zerovpn / cs_fnrm / cs_vpn / cs_epn / cs_dist. **TRAP (cost a debug cycle): storage_interface binding indices are GLOBAL PER SHADER FILE in declaration order — a kernel using a subset of a shared file's buffers gets a SPARSE descriptor set, which the Metal pipeline compiler REJECTS (`vkCreateComputePipelines result<-3>`). FIX: ONE shader FILE per kernel, each declaring ONLY its buffers in host-bind order (dense 0..N); drop unused buffers entirely (cs_epn references no p_* so it omits mif_par — declaring+binding an unused buffer risks DCE re-sparsifying).** AUTO-fit bound (extent<=0): position readback in writeParams; since writeParams runs PRE-dispatch (before the producer's compute on eval 1), it re-reads every eval and CONVERGES, never emitting a degenerate zero extent (placeholder until a valid fit lands — the live path heals in 1 frame; explicit extent skips it). DSL `mesh_to_sdf(src, dim=, extent=, center=, pad=, sign_mode=)`. GATE (test_sdfgrid.py, openvdb oracle): box + icosphere through the REAL graph vs `meshToLevelSet` built from the SAME readback geometry — 0 sign mismatches both, near-band distance err 0.375 voxel (box, sharp edges) / 0.089 voxel (icosphere); pseudonormal≡winding sign field (0 disagreements); DIRTY-input holey box → winding fallback lost 0/32768 voxels vs the closed box. **M2 LANDED 2026-06-12 — the boolean chain, live + watertight:** (a) `Csg` (sdfdflow_module_csg.cpp): union/intersect/subtract/smooth-union of two SDF bricks; output rides A's frame, B sampled TRILINEARLY at A's world positions (frames may differ). (b) `SdfToMesh` (sdfdflow_module_sdftomesh.cpp): **MARCHING TETRAHEDRA, not cubes** — each cell -> 6 tets sharing the main diagonal; a TINY hand-verifiable 16-case edge table (no 256-entry MC table to mis-transcribe) and tets are UNAMBIGUOUS (always watertight, no asymptotic decider). Winding made AUTOMATIC (each tri flipped to agree with the SDF gradient -> the table needs only correct topology). It is a hypermesh `MeshComputeInst` (mesh producer): the MC output size is data-dependent, so eval-1 classifies+scans the per-cell tri counts, `onTopologyReady` reads the total back + allocMesh's exact + requests the re-eval that EMITS (the subdivide cascade pattern); joins the standard triangulator/render/cook paths like any generated mesh. v1 emits per-cell SOUP (coincident verts on shared edges — provably watertight via bit-identical shared-edge crossings; the explicit MeshSort index-weld is the remaining refinement, NOT a correctness gap). DSL: `csg(a,b,op=)`, `sdf_to_mesh(src)`. GATE (test_sdfgrid.py): csg union/intersect/subtract vs analytic (~4e-7); sdf_to_mesh of a SPHERE and of the box∖sphere CSG CHAIN both = watertight genus-0 manifolds on RAW GPU floats (every edge shared by exactly 2 tris, **Euler χ = V-E+F == 2**, 0 slivers, sphere verts on-surface to 0.02 voxel). DEMO `sdf_boolean.py` (ork.hypermesh.viewer.py sdf_boolean) materializes box∖corner-sphere through the REAL viewer path (materialize_live, 27k faces). TRAPS: shadlang FUNCTIONS must live in `libblock`s (free functions between interface+shader -> 'no matching overloaded function') — INLINED instead (precompute the 8 corner pos/grad/sample arrays once per cell); `dump_obj` printed only 6 sig-figs (splits bit-identical shared verts -> false trimesh non-watertight) -> bumped to precision(9). M2-core complete; the MeshSort weld (shared/indexed verts) + a windowed visual confirm remain. **M0 LANDED 2026-06-12**: the `SdfGrid` interchange plug (the THIRD GPU-resource handle in dflow/interchange.h — dense v1: float SSBO x-fastest + dim/origin(voxel-CENTER convention)/voxel/background + _version; SdfRepr enum reserves NANOVDB) + the `sdf` family (ork.lev2 src/gfx/sdf — SdfModuleData/SdfComputeInst riding MeshEnv per the ratified v1 env contract) + `SdfEval` (authored GLSL distance expression of `vec3 p` BAKED into the kernel — the Select-predicate pattern; dim/extent/center = RUNTIME plugs, a dim poke reallocs the brick from the pow2 pool with no recompile) + registers in both hypermesh drivers + the python algebra `ork.hypergraph.dflow.sdf` (sphere/box/capsule, | & − operators, smooth_union, .offset — emits deterministic GLSL) + DSL chain verb `sdf_eval(expr, dim=, extent=, center=)`. GATE test_sdfgrid.py: C++ analytic oracle (EVERY voxel vs analytic for sphere + two-sphere-union: max err 2.4e-7; x-fastest layout; sign at known inside/outside; dim-poke realloc+recompute) + DSL emission spot-checks + byte-identical serdes with an SdfEval aboard. Hypermesh gates unaffected.**
- **E.8 GENERATOR-data editing in ecsedit** (owner ask, 2026-06-11 — the day `-e` first opened a
  generated scene). Three tiers: (1) TODAY — editing the LOWERED data (plugs/components/gendata
  scalars). (2) NEAR — "regenerate from recipe": the recipe already serializes
  (dsl_file/dsl_class/params on the gendatas); the editor edits the params varmap and re-runs
  the DSL → re-embed → re-materialize. The re-run machinery IS the retained model-A fallback
  path, and ecsedit is a Python app so Python-in-the-loop is legitimate THERE (the no-Python
  rule binds only the runtime host). Missing piece = editor UI + the regenerate verb. (3) FAR —
  structural editing of the DSL expression graph itself (the werkkzeug-style hypergraph canvas;
  the HyperSyn long game). FORCE-MULTIPLIER: the "prefer bindable" doctrine — every recipe param
  lowered to a runtime plug/Parameters entry is live-editable at tier 1 with NO re-trace; only
  genuinely structural params (loop bounds, topology counts) need tier 2. Pushing DSLs toward
  bindable shrinks the regenerate surface before any editor work happens.

---

## Deliberate divergences from a naive phase ordering

1. **2.18 / 2.17 / 2.15 pulled into Stages A/B** — they are contract/corruption items that look like later (Phase-2) work; "before a library accumulates" means before A.2 creates the library.
2. **P3b pushed BEHIND the Pivot-1 ports** (a naive ordering co-phases them) — surface-shrinking: the ports delete the readback paths P3b would otherwise have to sync.
3. **The gid CONTRACT split from the gid FEATURE** — the mask lock is S-sized insurance in B.5; the render bucketing waits in E.3 with zero rework.
4. **1.3 (EXPRP→plugs) pulled from Phase 1 into the B contract cluster** — so the A.1 reflected-vector form doesn't fossilize; assets are regenerable from .py today, so schema churn is free *now* and expensive later.

## Where "no regression" is genuinely hard — and the mechanism for each

| Step | Risk | Mechanism |
|---|---|---|
| B.1 typed canConnect | rejecting valid existing edges | WARN-mode soak across all family suites → then enforce |
| C.4 GPU ports | silent geometry drift | per-op A.4 equivalence oracle vs the retained CPU reference; raw-compare determinism gate; one op per landing, individually revertible |
| C.5 P3b | use-after-free corruption (ring/pool) | feature flag, blocking path as fallback; fence-gated recycling co-landed, never sequenced |
| D.1 C++ materializer | scene-load behavior drift | Python wire path delegates per-asset-type; old path removed only at parity |
| Everything | unnoticed serialization loss | the A.2 gate runs in CI from the moment it exists |

## Sequencing tensions left to the owner

- **Racer-track interleave.** Architecture-first means the racer looks unchanged until C.4-inset and E.3. The B.5(a) contract is cheap insurance either way; if a mid-plan visual win matters, pull **inset (C.4 third item + 2.6)** earlier within C — it is simultaneously the worst CPU path and the most racer-visible fix.
- **C vs D staffing.** They are genuinely parallel; if serialized, do D.1–D.3 before C.2–C.5 (Goal C is the project's stated endpoint and D is mostly mechanical), or C first if the 5090/perf story is the near-term demo.
- The B.5 decisions (fxv2 embed-vs-alias, dirty direction, transformer semantics) need owner calls — each is a one-paragraph decision, none blocks Stage A starting today.

---

**Bottom line:** A = build the ratchet (cheap, additive, makes everything else safe). B = lock the five contracts the whole future is authored against (small code, the only stage that serializes everything behind it). C ∥ D = the two hard streams — engine GPU-native core and the C++ host spine — each stepping behind its own oracle, riskiest change last and flagged. E = the features, which by then genuinely fall into place. First concrete move: **A.1, starting with `SelectData::describeX`** — the silent-catastrophe field — and the gate right behind it.

---

# Appendix A — Locked contracts & standing commitments (distilled from the retired docs)

*These are binding design commitments captured in `hypermesh_design_review.md` (Jun 8) and the May `HYPERECS_PLAN.md` before those docs were retired. They are NOT superseded — the plan items above implement them. The Tier-3 Scene-composite DESIGN itself stays in `HYPERECS.md` (same dir; still the live design doc for the unbuilt tier).*

## A1. The tag-space contract (LOCKED) — implements B.5(a), consumed by E.3

`__tags` (32 bits per face) = `[0:20)` **free-form** (operator working space; optional 5-bit-bank tooling, bank 0 = volatile op-scratch by convention) · `[20:32)` **persistent gid, LOCKED** (12-bit int 0..4095, single-membership durable identity — the material/semantic-class key; likely subsumes the `material_id` face channel).

- `gid = (tags >> 20) & 0xFFFu` · set: `tags = (tags & 0x000FFFFFu) | (g << 20)` · gid mask `0xFFF00000u`, free mask `0x000FFFFFu`.
- Implications: (1) `MaskOp`/`FaceTagger` writes are MASKED to `0x000FFFFF` so build ops can never clobber the gid (and it never moves); (2) two distinct write verbs — bitfield `MaskOp` (OR/AND/XOR, free region only) vs **`assign_gid(n)`** (int-set into the top 12) — plus the read atom `S.gid`; (3) ONLY the gid is locked; tactical+user bits are one uniform free region (the 5-bit bank is organizational tooling, not a contract).
- Per-face gid = material/semantic CLASS; per-INSTANCE identity/variation rides the instance-attr buffer — don't conflate.

## A2. The serialization principle (owner) — implements A.1, asserted by A.2

Serialize **precisely the state that changes what a GraphInst computes** — test every field by that. IN: connections (GraphData edges) · plug AUTHORED values (unconnected-input defaults) · plug input TRANSFORMERS (per-plug scale/bias/curve — easy to forget, silently changes results; the round-trip gate must assert these) · module member params (matrices/GLSL strings/masks/vectors). OUT: plug *structure* (name/type/dir — rebuilt by `reshapeIOs`), transient runtime state (computed values, RNG), imperative `onUpdate`. Reflection method is per-field: `directProperty` for POD, `lambdaProperty` for computed/string/validated (terrain `ExprModule.shadertext` is the string precedent). Trap: any new plug type must keep plug creation idempotent (reshapeIOs runs in createShared AND postDeserialize).

**Animation tiers:** (a) live Python `onUpdate` = app/authoring convenience, NOT serialized; (b) **declarative graph nodes** (Time/oscillator/op modules into param plugs) = the portable asset form; (c) external driving (game/physics) = the HOST pushes plugs. The C++ host's only per-frame job is feeding external inputs — chiefly the clock.

## A3. The ratified decision register (#1–#7 — closed; rationale preserved)

1. **Determinism:** deterministic STORED output is free — the renderer's contiguous CSR forces the ordered scan; raw-buffer compare is therefore VALID for all gates (no canonicalization); same-machine scope, cross-GPU bit-identicality a non-goal. Constrains the Pivot-1 ports to scan-scatter (not atomic-append) for stored output.
2. **City-scale architecture = the hierarchical cross-family hypergraph:** not one giant graph nor thousands of unique ones — a cross-family PIPELINE (`terrain → citygen → hypermesh`), a catalog of TYPES linked by two edge kinds: instance-source (scatter buffer → instancing) and field-input (heightfield → displace/conform). Two interchange flavors: expression-IR (the proven terrain↔ptex3d SurfNode spine) + **GPU-resource handles** (heightfield/ScatterSet/mesh/SDF — a mesh is a resource, not an expression). Prove two real links before generalizing (plan E.1/E.2).
3. **Portable form = the REAL `dflow::graphdata` serialized via reflection** — persist the trace OUTPUT (GLSL strings, baked matrices, flat vectors), never the recipe (the particles dsl_file/re-run-Python model is the named anti-pattern). Stable names survive round-trip; rebind-by-name is the host's driving mechanism.
4. **Booleans = the SDF domain** (`poly→SDF→CSG min/max→poly`), not polygon CSG. Requires the clean/watertight/manifold poly invariant → the triangulation floor + winding + manifold-weld are PREREQUISITES of the SDF bridge. Sharp edges = feature-preserving extraction (dual-contour w/ Hermite), deferrable.
5. **Acceptance gate = RACER FIRST** — hard-surface shaping + gid multi-material ahead of generic cross-domain; the racer is the persisted gate asset.
6. **Shipped runtime: discrete GPU first-class** — DEVICE_LOCAL residency shipped; every readback is a PCIe round-trip on discrete, so no-readback work is discrete-critical.
7. **Interactivity: undo is a NON-GOAL** (procedural — the .py/graph is the source of truth; history = version control). Interactive authoring = watch-mode hot-reload + runtime param plugs; cook-cache incremental re-eval is an optional large-graph latency optimization, deferrable.

## A4. Data-model debts (feed Phase 2/3 — quantified once, don't lose)

- **Per-semantic channel stride:** every channel is forced vec4/stride-16 (UV wastes 50%, scalars 75%; ~96+ B/vert before topology) — a *prerequisite* of any city-scale memory claim.
- **Named vertex-channel map:** the fixed `MeshChannel` enum is hardcoded at ~45 call sites — violates the carry-unknown-channels-forward constraint; B.5(b)'s channel-table convention for the Pivot-1 ports is the bridge.
- **Face-attr scatter through topology changes:** ops that alias `out->_faces = in->_faces` across count/order changes desync `material_id`/`__tags` (latent correctness bug).

## A5. Acceptance shapes carried from the May milestone plan (M→Stage mapping)

- M0 *Hello-Scene* (a cube renders through the ECS scene composite) → the precursor shape for **D.5**'s pure-C++ host demo.
- M3 *JSON round-trip* (serialize→reload→identical) → **A.2** (headless gate) + **D.5** (live keystroke gate).
- M4 *fragment composition* (multi-fragment render identical to single-fragment) → Stage E / Tier-3 work.
- *Sugar↔primitive equivalence harness* (the same scene authored via sugar and via raw primitives must lower identically — divergence is a bug) → ships WITH the Tier-3 scene composite (plan 3.5).
- Risk register highlights still live: sugar/primitive drift · two-pass lowering order · validator-subprocess library drift.

## A6. Standing Tier-3 design commitments (the live design = `HYPERECS.md`)

Still-operative commitments not restated elsewhere: the **three-tier authoring model** (Tier 1 standalone and Tier 3 scene composite coexist PERMANENTLY — Tier 3 never replaces Tier 1) · **stage-then-swap live mutation** (session.stage/revert + auto-revert ring; REALITY CHECK: the swap is 2–3 frames, not the spec'd single frame — barrier-protected, no tearing, but update the spec when implementing) · the **time namespace** (`Expr.sim.{time,dt}` / `Expr.entity.{spawn_t,age,pos}` / `Expr.slot.{t,dt}` / `Expr.ptc.{unit_age,random}`) — B.4's clock work must land names compatible with this · **fragment composition** (pass-`self`-to-helper as the composition primitive) · **string-keyed reflection lookup** in every type-resolution path (zero orkid-internal Python imports) · the **phase model** (INSTALL/STARTUP/SCENE_LOAD/PRELOAD/LIVE) is respected by scene-level materialization.
