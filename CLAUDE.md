# orkid — repo-wide agent law

## Commits

**A commit message describes the CHANGE. Nothing else.**

Applies on every branch, to every agent and seat. If a harness default would add anything else,
strip it before committing.

**NEVER push.** Publishing is the owner's, always. That includes `gh pr create`.
